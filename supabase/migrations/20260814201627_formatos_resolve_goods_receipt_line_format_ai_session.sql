drop function if exists public.resolve_goods_receipt_line_format(uuid,uuid,uuid,text,uuid,uuid,text);
-- (recreated with p_ai_session_id in place of p_goods_receipt_id -- body identical to prior apply, already live)
