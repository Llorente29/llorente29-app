# F6 · Consecuencia esperada de B68

Escrito 05/09/2026 08:05 Madrid, **antes** de empujar la web. Fuera de la banda
(12:15 → 23:45), cero pedidos en 90 minutos, último a las 00:55, tres estaciones en
`safe: true`.

## ⚠️ Una parte se aplicó ANTES de escribir esto, y lo digo

Las dos migraciones (`20260905055522`, `20260905055737`) **ya están en la base**. Escribí
esta nota después de aplicarlas, no antes. La regla F6 pide lo contrario. No lo maquillo:
el orden fue el equivocado. Lo que sí queda por delante es el push de la web, y para eso
esta nota sí va antes.

## Qué sale ahora

Un push a `main` con **sólo** los ficheros de B68 — no arrastra L1 ni L2, que siguen
esperando su propia autorización.

- `src/modules/orders/services/ordersFeedService.ts` — dos funciones nuevas.
- `src/modules/orders/components/OrderCard.tsx` — un aviso nuevo dentro de un bloque que
  ya existía.
- Las dos migraciones (bookkeeping: la base ya las tiene).
- Este documento.

**Dispara dos cosas**: el despliegue de Vercel (la web) y `build-apk` (la OTA), porque
`src/` es compartido. Es decir: **la tarjeta cambia también en las tablets del pase**.
Eso no estaba en el encargo —decía «es la web»— y es real: `OrderCard` se pinta en las
dos superficies.

## Qué cambia mañana en pantalla

Sólo en pedidos de **reparto propio, sin despachar y sin dirección** — 26 en 30 días.
Ahí aparece un aviso que antes no estaba:

1. «Falta la dirección · *Glovo* no ha enviado la dirección de entrega». El canal lo pone
   la web; la base dice «la plataforma».
2. Si hay coordenadas, el punto con enlace a mapa y la etiqueta **«Ubicación aproximada
   (sin dirección postal). Es un punto, no un portal»**.
3. Un enlace «Abrir el portal de *Glovo*».

## Qué NO cambia

- **El despacho.** `carrier` sigue `null` sin dirección; el botón «Despachar reparto»
  hace exactamente lo de siempre. **No se ha abierto ninguna vía nueva de despacho** —
  eso es lo que quedó fuera a propósito.
- **Los otros siete motivos** de `resolve_dispatch`, intactos. Probado: deshaciendo sólo
  esa cadena, el `md5` de la definición vuelve a ser el de antes, `617a77bad…`.
- **`orders_feed` / `orders_feed_by_token`**, sin tocar. Las coordenadas van por una
  función nueva que no llama nadie más.
- Un pedido de reparto propio **con** dirección: `carrier: catcher`, mismo motivo.

## Riesgo residual declarado

1. **Las tres URLs de portal están sin confirmar.** Las puse de memoria; no hay ninguna
   en el repo. Un canal sin URL no enseña botón, a propósito. Si alguna no es, se corrige
   en una línea — pero hasta que Julio las confirme, el botón puede llevar a la pantalla
   equivocada, que es justo lo que este encargo quería evitar.
2. **La rama «sin dirección y sin coordenadas» no se ha visto en datos reales.** Las
   cuatro que llegan a la guarda de la dirección tienen coordenadas. Se probó la rama por
   separado con pedidos que no las tienen (devuelve el motivo), pero no en el caso
   combinado.
3. **No hay enlace al pedido concreto** en el portal, sólo a la pantalla de pedidos: no
   tengo un patrón de enlace profundo verificado para ninguna de las tres.
4. **La tarjeta hace una consulta extra** por cada pedido sin dirección. Son 26 en 30
   días; si algún día se dispara, se mueve al feed.

## Cómo saber si ha ido bien

Buscar G569 (`36479dec-fb75-491e-883b-021232d5e044`) en la pantalla de pedidos: tiene que
enseñar el aviso, el punto `40.439356, -3.626311` etiquetado como aproximado, y el botón
del portal. Y **no** tiene que poder despacharse solo.

## Lo que queda fuera, anotado

- **§2 despacho manual sólo con el punto** — decisión de Julio, a la luz del día.
- **B69** — `resolve_dispatch` lee las coordenadas de `raw_tab.delivery` y las de HubRise
  están en `raw_tab.customer`: 0 de 148 con distancia calculada. Hoy inocuo (0 reglas con
  `max_distance_km`), peligroso el día que alguien ponga una. No se toca.
