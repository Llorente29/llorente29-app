# Regla F6 · Consecuencia esperada de la salida de C9 Lote 1

**04/09/2026, 10:40 Madrid.** Escrito ANTES de desplegar, que es de lo que trata la regla.

## Ventana

Banda prohibida 12:15 → 23:45. Ahora son las **10:40**: quedan **1 h 35 min** de margen.
Si algo se tuerce y no da tiempo a verificarlo, **se para y se retoma después de las 23:45**;
no se apura la banda.

## Qué sale, exactamente

Fusionar mi rama a `main` es **fast-forward** (`origin/main` ya es ancestro; comprobado con el
historial completo tras `fetch --unshallow`). Dispara tres cosas a la vez:

| workflow | qué despliega |
|---|---|
| `deploy-edge-functions` | `label-scan` (nueva), `order-advance`, `system-alert` |
| Vercel (`folvy-app`, rama `main`) | el rewrite `/E/:token` en `foodint.folvy.app` |
| `build-apk` | el **bundle OTA** a las tablets (sin APK: no hay gatillo nativo) |

> `deploy.yml` NO entra: dispara en la rama `source`, que va 1.671 commits por detrás. Está muerto.

## Consecuencia esperada, pieza a pieza

**1. `label-scan` (nueva).** Nadie la llama todavía salvo el rewrite. Consecuencia: ninguna.
Comprobada con `deno check`: limpia. Se le añadió el `deno.json` que le faltaba — sin él, el
especificador `@supabase/supabase-js` no resuelve y el despliegue habría fallado.

**2. `order-advance`.** Camino vivo de los pedidos. Cambios: acepta el secreto anterior durante
la rotación (B55) y elige la integración por orden determinista (B54). **Consecuencia esperada:
ninguna** — hoy las dos filas activas de Foodint apuntan al mismo token, y
`ORDER_ADVANCE_SECRET` no ha cambiado. Sin regresión de tipos: con el mismo stub, la versión de
`main` da 4 errores y la nueva 3.

**3. `system-alert`.** Sólo cambia el pie del correo (B61). Consecuencia: los avisos dejan de
decir «kind sin mapear».

**4. Vercel.** `/E/<token>` y `/e/<token>` pasan a redirigir 302 a la tienda. **Antes de esto,
un QR nuevo daría en el catch-all del SPA.** Por eso el orden importa: la web tiene que estar
desplegada antes de que se imprima la primera etiqueta con token.

**5. OTA.** Las tablets reciben el renderizador nuevo. **A partir de ese momento, las etiquetas
llevan el QR con token** — versión 3, 29 módulos, ECC Q, 21,8 mm de lado a módulo 6.
`mandatory:false`: se aplica en la ventana segura de cada estación, sin interrumpir el pase.

## Lo que NO cambia

- Las migraciones ya están aplicadas (08:26 UTC) y son **compatibles hacia atrás**: una tablet
  con el renderizador viejo ignora `unit_tokens` y `bag_token` y sigue imprimiendo el QR de
  marketing. Por eso se pudieron aplicar antes de la OTA sin riesgo.
- Nadie enciende `labels` en Carabanchel. Eso sigue siendo decisión de Julio (§4 del encargo).

## Si algo bloquea la OTA

La guarda es `station_update_window`, y tiene **B52 abierta**: da por vivos los pedidos en
`rejected` y `delivery_failed`. Si una estación no se actualiza, mirar ahí primero —
`select * from station_update_window('<token>')` dice el motivo en cristiano.

## Riesgo residual declarado

`order-advance` y `label-scan` **no se han podido comprobar contra la librería real**: el proxy
de este entorno devuelve 403 a `jsr.io`. Se comprobaron con un stub local, que valida mi código
pero no los tipos de `@supabase/supabase-js`. La comparación contra `main` con el mismo stub es
lo que permite afirmar que no hay regresión — no que no haya errores preexistentes.
