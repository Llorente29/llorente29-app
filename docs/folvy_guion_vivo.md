# Folvy — Guion vivo (qué hacer, por impacto comercial)

> **Última actualización**: 28 jun 2026 (CIERRE 8 — ESCANDALLOS: (1) **B2 ARREGLADO DE RAÍZ** — la pantalla de revisión anti-duplicados al importar ficha seguía duplicando ingredientes nuevos; raíz real: la decisión del humano vivía solo en `mapping_proposal`, que a veces no tiene la fila → se perdía en silencio → materialize creaba nuevo. Solución: la decisión se pasa DIRECTA a `materialize_recipe_session(p_session_id, p_decisions)` y manda; verificado en vivo con reimportación (0 duplicados). Migración `20260628T1700`. (2) **UNIDADES NO CONVERTIBLES — fuga silenciosa de stock**, saneada en datos (26→0 líneas): conversiones hamburguesa 1ud=85g, gouda 1ud=20g, rulo 1ud=40g, y Pasta Trufada con base mal puesta (ud→g). FRENTE DE NÚCLEO DECLARADO (sesión dedicada): BLOQUEAR la línea con unidad no convertible (decisión Julio), 6 piezas, RECON de los 3 motores hecho (`explode_recipe_to_raws` salta en silencio). (3) **DUPLICAR RECETA** en producción: RPC `duplicate_recipe_item` (plato+líneas+pasos+foto atómico) + botón + título editable. Migración `20260628T1900`. (4) **FRENTE DE NÚCLEO DECLARADO: "Eliminar no elimina"** — plato confirmado borrado sigue vivo (archived_at NULL); RECON de las 2 funciones hecho; siguiente paso = reproducir con consola; NO tocar DELETE+CASCADE en caliente. Commits hasta a0b0ca7 (rev-list 0 0). DEUDA: regenerar `database.ts` (CLI roto, 2 casts puntuales). — Lo anterior: 27 jun 2026 (noche-2 — CIERRE 7: FOLVY SHOP NÚCLEO TRANSACCIONAL COMPLETO Y VERIFICADO EN VIVO. (A) PAGO STRIPE CONNECT completo y probado con tarjeta 4242: direct charges sobre la cuenta conectada del restaurante (dinero a SU cuenta; Folvy cobra application_fee=total×shop_fee_bps/10000), Edge `shop-payment-intent`+`stripe-webhook` (firma verificada, mark_shop_order_paid mueve new→accepted server-side), Payment Element en checkout, ONBOARDING REAL desde panel admin (Account Links API, guard is_platform_admin) + comisión % sin SQL. Plataforma test Fplvy acct_1TmvYm; conectada Llorente29 acct_1TmwGv. (B) MÉTODOS DE PAGO configurables por cuenta (shop_pay_online/cash_pickup/cash_delivery + shop_payment_config(slug)); decisión Julio: efectivo se acepta AUTOMÁTICO (nace accepted, dispara impresión+Catcher), online ESPERA confirmación de pago (webhook); feed/kds ocultan folvy_shop en 'new'; botón efectivo en checkout; etiqueta \\\"Recoger en el local\\\". (C) CADUCIDAD de abandonados (expire_unpaid_shop_orders, cron 5min, cancela online en 'new' sin pagar >30min; no toca efectivo). (D) FIX selector de locales (timing de sync, mostraba estado engañoso). (E) TABLET+IMPRESIÓN verificadas con HARDWARE REAL (Tablet J Plaza Castilla, NT311, 3 tickets bag/kitchen/labels; imprime al ACEPTAR). (F) CONSUMO STOCK/AvT motor OK y agnóstico pero BLOQUEADO POR DATOS: los 107 menu_item con recipe_item_id apuntan a recipe_items VACÍOS; escandallos reales sin enlazar al catálogo → frente transversal catálogo↔escandallo (afecta a TODA venta, no solo Shop). (G) NOTIFICACIÓN cliente: arquitectura agnóstica de canal diseñada, APARCADA (dependencia: proveedor WhatsApp); costes investigados (WhatsApp utility 0,0166€/msg+49€/mes BSP 360dialog; SMS A2P 0,05-0,09€ caro y NO usa tarifa personal; email casi gratis Resend). (H) HALLAZGO CRM: el teléfono de pedidos lastapp es PROXY de plataforma (no identifica cliente); CRM real solo desde canales propios; direcciones/horas SÍ para BI geográfico agregado (legal); idea Julio owned-channel migration (detalle físico→consentimiento RGPD). Commits e2de5bc/baf9b08/75ea725/638ed8d (+ pago previos b117c8b/ce5bc5e/1254de3), rev-list 0. DEUDAS PRODUCCIÓN STRIPE: Bizum+quitar iDEAL/Klarna en la conectada, plataforma DEFINITIVA Folvy SL, webhook de producción, comisión por defecto clientes nuevos, ROTAR secrets de test. — Lo anterior: 27 jun 2026 (noche-1 — FOLVY SHOP de escaparate a CASI-COBRA. (1) HORARIO COMERCIAL = MÓDULO COMPLETO (pieza transversal que NO existía): tablas `business_hours` + `business_hours_exception`, función canónica `is_brand_open(location_id, brand_id, ts)`; editor reutilizable por (local, marca) con herencia del defecto del local (brand_id NULL); copiar horario entre marcas/locales; excepciones/festivos POR RANGO de fechas; vista gráfica (rejilla 7d×24h); aviso \\\"comercial abierto sin personal\\\" en ficha de local Y en cuadrante, vía `hours_staffing_gaps` que cruza horario vs cobertura del cuadrante VIGENTE. BUG GRAVE cazado y resuelto: el cuadrante usa 0=Lunes, Postgres dow 0=Domingo → el cruce mentía; fix `(dow+6)%7`; el cuadrante NUNCA tuvo bug. Commits 65869c7/3c3a8d1/d8c9a85/fa406bb/81f64ab. (2) FOLVY SHOP — estado ABIERTO/CERRADO en Hub y carta (la \\\"puerta honesta\\\", vía is_brand_open; cerradas atenuadas). (3) MODAL DE CONFIGURACIÓN DE PLATO: RPC `shop_item_config` (combos con slots + modificadores ANIDADOS por opción + ALÉRGENOS REALES por recipe_item — cumplimiento legal, golea a Last que pone \\\"contiene alérgenos\\\" genérico); modal panel partido, precio en vivo, validación min/max. (4) CARRITO CROSS-BRAND COMPLETO: contexto persistente (localStorage por slug), REGLA MISMO LOCAL = una entrega (patrón Otter/Glovo; marcas de Llorente29 en 3-6 locales, la regla es crítica), panel \\\"Tu pedido\\\" agrupado por marca, mini-panel de confirmación tras añadir con cross-brand DESTACADO (Pagar primario / Ver otras marcas secundario destacado / Seguir discreto). Anclas previstas (no construidas): descuentos/promos, pago multi-método (Bizum vía Stripe), cliente/fidelización, \\\"te faltan X€ envío gratis\\\". (5) CHECKOUT — pasos ENTREGA y HORA construidos, ESTILO B nuevo (moderno tipo Glovo/Uber 2026, una sola página, responsive móvil con barra inferior desplegable; Julio eligió B sobre el coherente-con-Hub; selección en TINTA, coral solo en el botón final). ENTREGA: modo domicilio/recoger, AUTOCOMPLETE de dirección con debounce (sin botón Buscar, reutiliza geocodeAddress de Mapbox del editor de zonas), validación de zona vía `shop_check_delivery` (PostGIS ST_DWithin por radio), pedido mínimo. HORA: \\\"Lo antes posible\\\" con ETA real + \\\"Programar\\\" con DESPLEGABLE nativo (no parrilla de botones, que Julio rechazó por anticuada), franjas de 30 min solo hoy que respetan el horario comercial vía `shop_delivery_slots`. Commits 6c31b35/3f78333/3b0dbfe/3ca455a/18b3744/486abf7/334843e. RPC versionadas. **DECISIÓN DE DISEÑO PENDIENTE (Julio lo usa unos días): MIGRAR TODO EL SHOP a estilo B** — viste maquetas de Hub y checkout en B y te gustó más; si confirma, frente propio \\\"sistema de diseño B + migración del Shop\\\" con maqueta por superficie antes de tocar (Hub, carta, modal, carrito ya existen en coral+crema).) — Lo anterior: 25 jun 2026 (noche — REPARTO A CATCHER + HUB PÚBLICO DE FOLVY SHOP). (A) CATCHER (reparto last-mile) COMPLETO Y VALIDADO EN SANDBOX: Edge `catcher-dispatch` (auth + create order v1 a staging-api.catcher.es, validado real con orderId de Catcher) + `catcher-webhook` (cruza por externalId, guarda delivery_state/rider/transport_price) + disparo automático por trigger `tg_auto_dispatch_catcher` (solo source=folvy_shop AND accepted AND sin carrier AND modo auto; frontera por secreto interno). Conector Catcher conectado en producción (local Alcalá). Despacho transversal: Folvy solo despacha donde Last NO lo hace. DEUDA: hoy solo dispara con folvy_shop; al entrar Llorente29 en HubRise → pasar a configurable por canal. Fix colateral: activación de conectores tipo credentials (creaba la conexión al guardar). (B) HUB PÚBLICO FOLVY SHOP (`/t/:slug`, escaparate multi-marca, diseño "fresco urbano" tipo Glovo modo claro APROBADO por Julio): RPC `shop_hub_by_slug` v3 (hero/tagline de cuenta + marcas con logo/foto/rating semilla + delivery_info), página `ShopHubRoute.tsx` (hero con imagen real de portada, franja info, chips categoría, "lo más pedido", rejilla con logos reales+badges, carrito lateral). Slug de Llorente29 = "foodint" (su marca pública). Imagen de portada subida a Storage. Commit 2f6e17c (rev-list 0). **DEUDA HUB (mejoras que Julio dejó pendientes "muy a su pesar", a retomar):** datos de MUESTRA por conectar a real (chips no filtran, "lo más pedido" fijo, valoraciones/categorías/badge Gratis inventados, carrito NO funcional, buscar/entrar/dirección no funcionales); publicar las 17 marcas (hoy 8); afinar layout (Julio no convencido del aspecto final, "hemos ido para atrás"). Siguiente ladrillo natural del Shop = carta de marca `/t/:slug/:brandId`, luego checkout+pago Stripe, pedido Shop→KDS+stock. MODELO VALORACIONES aprobado (frente futuro): semilla importada de Glovo/Uber (campos seed_rating/seed_rating_count ya creados) + reseñas propias que la diluyen con el tiempo; sistema de reseñas propio = frente aparte. HORARIO COMERCIAL DE TIENDA (abierto/cerrado al cliente) NO EXISTE en BD (schedules es cuadrante de PERSONAL) → frente futuro. — Lo anterior: 24 jun 2026 (noche — TICKET DE FACTURA/BOLSA v2 = MOTOR DE IMAGEN, clon de Last EN VIVO). El ticket de texto ESC/POS tenía techo de calidad y la NT311 CORROMPE € y acentos por codepage (probado CP858/WPC1252/0xD5, todos fallan). Decisión Julio (innegociable): el ticket de Folvy debe ser INDISTINGUIBLE de uno de Last. Solución: renderizar la factura/bolsa como IMAGEN (canvas `@napi-rs/canvas`, fuentes DejaVu incrustadas) y mandarla como RÁSTER ESC/POS (GS v 0, tiras de 128px) — € y acentos perfectos, tipografía real, AGNÓSTICO de impresora. Validado en papel (Milanesa House reparto propio): logo protagonista sin deformar (autocrop + escala proporcional, tope 190px para cuadrados), fiscal, banda del código, dirección de cliente DESGLOSADA en campos (Dirección/Detalles/Código postal/Teléfono de `raw_tab.delivery`), productos con tachado de descuento, tabla IVA (10% hostelería, legal), Total grande, QR de la marca y PIE PUBLICITARIO Folvy. PIEZAS SQL nuevas (vivas en BD, SIN versionar): `sale_line.original_unit_price`+`discount_label`, `fill_line_discounts` (descuento por línea canónico Last+HubRise), `order_for_print` (llama fill + devuelve descuento + `delivery_detail`), `fiscal_for_print`. AGENTE v3 (fuera del repo): `ticketImage.js` nuevo + `folvy-print-agent.js` v3 (factura por imagen; cocina/pegatinas siguen por texto); deps nuevas `@napi-rs/canvas`+`qrcode`. PENDIENTE: versionar TODO el SQL de impresión; botón de reimpresión; pasar cocina/pegatinas al motor de imagen; numeración VeriFactu. — Lo anterior: 23 jun 2026 (CIERRE 4 — SESIÓN MIXTA: integración cliente 2 + fix Orders + verificación HubRise + arranque carga de costes. (A) CLIENTE 2 (Kitchen Grill LstQ, `f69e10fa`) INTEGRADO EN LAST EN VIVO: tienda Ensanche Vallecas (`ff12b2bf`) vinculada en `external_location_map`→cuenta del cliente 2, integración Folvy instalada en la tienda desde el panel de Last ("Estás en línea con Folvy"), aparece ya en el integrador (7 ubicaciones). VENTAS AISLADAS POR TIENDA verificado en el código del webhook (`lastapp-webhook` resuelve `account_id` por `locationId` vía `external_location_map`, no por marca) → las ventas del cliente 2 entran a su cuenta sin mezclarse con Llorente29. BUG ABIERTO (no toca ventas): la pantalla de "marcas pendientes" del cliente 2 muestra marcas PROPIAS de Llorente29 (Foodint) porque el importador/listado pide el catálogo POR ORGANIZACIÓN (compartida en Cloudtown) en vez de POR `locationId` — confirmado contra la API de Last (`/catalogs` exige `locationId`; `/organizations/{org}/catalog` mezcla). Mismo patrón que el bug de combos ya cazado. NO importar catálogo del cliente 2 hasta el fix. (B) FIX FOLVY ORDERS — DÍA DE NEGOCIO CON CORTE 04:00, EN PRODUCCIÓN: lo que parecía "faltan pedidos en Orders" NO era bug (todas las ventas en BD con `order_status` correcto, Sales las cuenta bien; el descuadre era reparto por LOCALES distintos + la ventana móvil de 6h de `orders_feed` que escondía lo cerrado hace >6h). Verificado en BD ANTES de tocar (evitó un parche innecesario al backfill que Code dedujo por código). Cambio: la pestaña Cerrados pasa de ventana de 6h a DÍA DE NEGOCIO de las 04:00 a las 04:00 (TZ de la cuenta = `accounts.timezone`='Europe/Madrid'); vivos sin límite, terminales solo si cierran dentro del día de negocio en curso. Constante `c_business_day_cutoff_hours=4` legible (no configurable aún). Migraciones `20260622T1700_orders_feed_cerrados_dia_local.sql` (día local) + `20260623T0900_orders_feed_dia_negocio_corte_0400.sql` (corte 04:00). Validado en vivo: pasada medianoche, Cerrados sigue mostrando 31 del día. (C) ADAPTADOR HUBRISE VERIFICADO CONSTRUIDO Y DESPLEGADO: RECON de Code confirma `hubrise-webhook` ACTIVE v11, `verify_jwt=false`, valida `X-HubRise-Hmac-SHA256` (hexdigest, timingSafeEqual), mapea order create/update a la ingesta canónica (`adapt_hubrise_order`/`close_sale`/`cancel_sale`), pone `order_status`, auto-aceptación fase 2; `hubrise-order-status` también ACTIVE. Pendientes = comprobaciones de campo con tráfico real (connection_name por marca, completed de Uber/JustEat, cadenas de canal), no falta build. Correo de seguimiento a Janaina (HubRise) preparado pidiendo SOLO bloquear la suscripción en la cuenta de test "Folvy Test (zy9j2)" (usuario partners@folvy.app), aparcando Glovo España. (D) ARRANCADA LA CARGA DE COSTES DE LLORENTE29: fuente de verdad decidida = informe "Compras y Ventas" de Cloudtown (lo que CTB paga de verdad), NO el export de tspoon (coste teórico, con datos rotos: harissa 0,015€, milanesa 233,96€). 5 meses de PDF (ene–may) cruzados contra los 150 productos de Folvy (raw+packaging+tool). Generado `Folvy_vs_CTB.xlsx`: ~14 ingredientes sin coste que CTB sí tiene, listos para cargar; resto = sub-recetas (Arroz criollo, pico de gallo, salsas que cocinan) o marca propia. FEATURE confirmada por Julio con caso de uso: ALARMA DE PRECIOS cuando una recepción supera el coste de referencia de CTB, aunque la venta venga de marca cedida. CARGA NO EJECUTADA (quedó en la tabla de revisión). — Lo anterior (CIERRE 3 · 21/06 — ESTACIÓN DE TABLET + IMPRESIÓN FÍSICA AUTOMÁTICA, dos hitos en producción/vivo. (A) ESTACIÓN DE TABLET (`/estacion`): terminal de cocina a pantalla completa por TOKEN de dispositivo (mismo `kds_device` que el kiosco), 3 pestañas SIEMPRE visibles —Pedidos · Cocina · Disponibilidad/86— abre por defecto en Pedidos; ruta pública montada en App.tsx ANTES de los gates de sesión (hermana de `/cocina-tv`). 3 capas: contenedor `TabletStationRoute.tsx` (calcado de `KdsKioskRoute`; la pestaña Cocina monta `KdsBoard` con token); Disponibilidad/86 por token (RPC `set_product_availability_by_token` + `availability_panel_by_token` que NO delega en la versión con guard de sesión sino que replica el SELECT validando solo token, + `search_products_by_token` + `preview_scope_by_token` + `device_location_by_token`; front `tabletAvailabilityService.ts` + `TabletAvailabilityTab.tsx`); Pedidos por token (`orders_feed_by_token` + `set_order_status_by_token`; `OrdersFeed.tsx` acepta token, vive del polling 10s). EXTRAS: QR+URL de la estación en `DevicesSettings.tsx` (lib `qrcode`); `manifest-estacion.json` propio (start_url=/estacion, tema oscuro) apuntado dinámicamente → "Añadir a inicio" crea un icono que abre la estación, no la raíz con login. Commits hasta `43695e6` (rev-list 0). LECCIONES: el navegador no abre sockets TCP; PWA start_url global=/login y el manifest dinámico lo resuelve; una RPC con guard `auth.uid()` no se delega desde otra RPC por token (replicar el SELECT); logos con texto azul se funden en fondo oscuro → isotipo+texto blanco. (B) IMPRESIÓN FÍSICA AUTOMÁTICA FUNCIONANDO EN VIVO: cadena completa validada —pedido se ACEPTA → trigger `tg_auto_print_on_accept` encola → agente lee por token → ESC/POS → NT311 por LAN → papel— SIN nadie con Folvy abierto. Arquitectura AGNÓSTICA multi-transporte (`printer.transport`: sunmi_cloud, escpos_network [montado hoy], epson_epos, bluetooth, browser_pdf; `printer.config` jsonb lleva ip/port/sn; NO depende de Sunmi). Modelo en BD: `printer` + `print_job` (payload jsonb INMUTABLE) con RLS calcada de `kds_device`. Agente Node.js en `C:\folvy-print-agent` (FUERA del repo): `escpos.js` (TicketDoc→bytes, corte GS V 1, ancho 48), `ticketRenderer.js` (PORT JS del `.ts` del front; el agente DIBUJA, no la BD = no se duplica lógica = Camino 2), `folvy-print-agent.js` (claim → si `payload.mode=by_order` pide el pedido con `order_for_print` y dibuja → TCP 9100 → report). RPCs (GRANT a anon): `claim_print_jobs` (FOR UPDATE SKIP LOCKED), `report_print_job`, `enqueue_print_job` (manager), `order_for_print` (un pedido por token, espejo de `orders_feed_by_token`). Trigger en `sale` AFTER UPDATE: al pasar a 'accepted' (no 'new': Glovo cancela antes) encola un job ligero por impresora×doc_type; solo dispara con cambio real de estado. Impresora registrada `0a0ada19-...` "NT311 Plaza Castilla" {ip:192.168.1.86, port:9100}. CLAVE: el proyecto usa el formato NUEVO de claves Supabase (`sb_publishable_...`; la legacy `eyJ...` da 401). La NT311 imprime en LAN sin cloud (Ethernet, puerto 9100); doble-click en pairing imprime su IP. **DEUDA CRÍTICA DE ESTE CIERRE (primero a hacer al retomar): NADA del SQL de hoy (printer/print_job + RLS, las RPC del agente, order_for_print, el trigger, y las RPC by-token de la Estación) está versionado en `supabase/migrations/` — solo vive en BD; y el agente vive FUERA del repo. Regenerar `database.ts` (printer/print_job nuevas).** AHORA: afinar layout de los 3 tickets (bolsa/cocina pequeños/ilegibles, pegatinas sin alérgenos) + botón de reimpresión/manual. Sunmi partner SOLICITADA (Llorente29 Food/Spain/Folvy), en revisión, para el transporte cloud futuro. — Lo anterior (CIERRE 2 · 21/06): CATÁLOGO LLORENTE29 COMPLETO desde Last. Frente que Julio declaró BLOQUEANTE de todo ("sin cartas fiables no se avanza a HubRise/86/escandallos"). Importador `scripts/import-last-catalog.mjs` (genérico --account+--org, dry-run/--run, idempotente por matrícula, --catalog-overrides, --debug). 4 BUGS cazados y arreglados: (1) cruce de marcas + sin categoría (el "paso de componentes de combo" inyectaba productos del ORG catalog con course genérico que mezcla marcas → ELIMINADO; categoría SIEMPRE de la membresía del catálogo BASE /catalogs/{id}), (2) PRODUCTO COMPARTIDO entre marcas (Coca Cola/Fanta/Tres Leches, misma matrícula en N marcas, solo se creaba en la 1ª → clave compuesta pkey(marca,matrícula) en TODO el flujo; un producto físico→N menu_items por marca, external_id=matrícula común; recipe_item compartido = eslabón siguiente; modelo nuclear "86 una vez apaga en todas"), (3) combos vacíos slot opciones:0 (p.productId YA ES la matrícula, se convertía vía orgProductById→∅ → usar directo), (4) app no mostraba combos en su categoría (brandCatalogService filtraba product_type='item' → quitado; combos viven en su menu_category_id, foto, editables, chip combo+N slots). MÉTODO validado y aplicado a ESCALA por org (borrar transacción → --run org → verificar sin_categoria=0 → recast). RESULTADO: 17 marcas Llorente29 completas y fiables idénticas a Last (8 cedidas Cloudtown b7bc4753 + 9 propias Foodint 31f13f35), TODAS sin_categoria=0. Casado 93,8%→87,8% estable (descasado = productos descatalogados con matrícula vieja; dato de venta conservado, recuperable). Commit 24f7961 (rev-list 0). DESBLOQUEA 86 + HubRise cliente 2 + escandallos faltantes. Para cliente 2: mismo importador sirve para sus cedidas CTB (misma org Cloudtown). PENDIENTE: detalle del combo no lista aún sus componentes (ficha dice "sin modificadores", follow-up fácil). — Lo anterior (CIERRE 2 · 19/06): FRENTE DE IMPRESIÓN DE TICKETS, primera fase en producción. Diseño aprobado `docs/folvy_impresion_diseno.md`: capa agnóstica ESC/POS + adaptadores de transporte; 3 documentos (bolsa con logo+QR, cocina por categoría sin alérgenos, pegatinas opción (c)). HECHO: D1 logo por marca (`brand.logo_url`+bucket+uploader), D2 shop_url+qr_caption por marca, D3 iconos UE (14 PNG, fix `soybeans`→`soy`), `orders_feed` ampliado (marca+`recipe_family`+combo_item con familia; migraciones T1600/T1700), renderizador capa 1 (`ticketRenderer.ts`: 3 docs texto+QR; pegatinas aplanan combos y expanden qty), preview en pantalla (`TicketPreviewModal.tsx`), botón impresora en `OrderCard`. Validado en vivo (combo DÚO=4 pegatinas, multiplicador 2x=2 pegatinas). Commits cc9a944, 963ffbd, 30f2e49, 59dc419 (todos rev-list 0). DEUDAS declaradas → frente siguiente: (1) CÓDIGO DE PLATAFORMA REAL (el #XXXXX es recorte de UUID, no el G406 Glovo/código Uber-JE; extraer de `raw_tab`→columna `sale.platform_order_code`→feed→ticket; OJO no uniforme entre plataformas), (2) capa 2 imágenes (logo+iconos a bitmap ESC/POS), (3) modelo `printer`/`print_job`, (4) adaptador Sunmi NT311 Cloud Partner. — Lo anterior (CIERRE 1): MÓDULO FOLVY ORDERS, cierre mayor: feed operativo multi-canal por el que entra y se opera TODO pedido (propias+cedidas), validado en vivo con Glovo. CERRADO: #1 ruta completa + empuje a Last (trigger→Edge), #2 escandallo al pulsar (Cook Mode del KDS), #3 marcar línea sincronizada con el KDS, #7 puente KDS→feed = ARQUITECTURA DEFINITIVA DEL EMPUJE (Opción A: el empuje es consecuencia del cambio de `order_status` vía trigger `trg_sale_push_status` + `net.http_post` fire-and-forget a Edge `order-advance`; FUNCIONA DESDE EL KIOSCO DE COCINA sin usuario logueado), 7a ciclo de vida por tipo de reparto (plataforma cierra al recoger el rider sin empujar el cierre; pickup/propio con sus botones). Commits ba89312, d70dae4, 63d2c54, 87c1a4e, b885e48, 0492702, ae88d20, 0f6cce8, 6c1d80d. Toggle de empuje activo en producción (Foodint + Cloudtown). DECISIONES: UNA tablet Folvy para propias+cedidas; escandallo de cedidas OBLIGATORIO (Folvy controla su stock+pedidos); toggle de empuje por organización; migración de escandallos cedidos Folvy Interno→Llorente29 = frente pendiente (sesión dedicada, entre cuentas). PENDIENTE Orders 100%: #4 cierre anti-faltantes, #5 impresión, #6 modificadores flag real, #8/9 cruce+collection, 7b reparto propio (flota Catcher/Jelp/Shipday + 'En ruta' + métricas). — Lo anterior: OVERRIDES (precio/margen por canal) EN PRODUCCIÓN + CICLO DE VIDA DEL PEDIDO fase 1 EN PRODUCCIÓN, validado en vivo (#RP65P received→accepted en HubRise) + diseño en papel de la pestaña Pedidos. (1) OVERRIDES: motor SQL `menu_item_channel_economics` (una fila por canal, precio override‖base, comisión por canal, margen neto real al céntimo), `set/clear_menu_item_override`, `menuOverrideService.ts`, modal "Editar precios" con margen en vivo por canal; cálculo cliente jubilado; migración `20260618T2300`, commit c1b6d4a. (2) CICLO DE VIDA fase 1: `sale.order_status` (estado de plataforma, separado del status contable) + CHECK + siembra desde raw_tab.status (migración `20260618T2400`); webhook espeja order_status en update/cierre/cancelación; Edge de SALIDA `hubrise-order-status` (PUT a HubRise con X-Access-Token, autoriza por RLS, espeja solo si HubRise da 2xx); token OAuth de la location de pruebas en Secret vía flujo oob; commit 5f78aa7. (3) DISEÑO pestaña Pedidos (`docs/folvy_pedidos_pestana_diseno.md`, commit 042a506): feed + detalle con contenido/alérgenos + acciones del ciclo de vida con reglas por plataforma + auto-aceptación por canal/marca. Investigación HubRise volcada a `docs/folvy_catalogo_publicador_diseno.md` (commit a19b232). Frente activo = construir la pestaña Pedidos (fase 2). DEUDA declarada: token multi-location = `hubrise_integration` (P-A/CP2). — Lo anterior: SESIÓN MÓDULO ALMACÉN COMPLETO + AvT + NIVELES (MRP II) + FICHAJE RESUELTO. Todo en producción, build verde, rev-list 0. (1) MÓDULO ALMACÉN cerrado: 6 secciones (Resumen con tarjetas vivas, Existencias, Niveles, Movimientos, Inventarios, Teórico vs Real) + ficha del artículo con stock/niveles/movimientos. (2) AvT (Teórico vs Real) — EL GRAN DIFERENCIADOR — en sus dos formas: PUNTUAL (último conteo aprobado, desviación €, salud del dato, causa probable) y POR PERIODO CONSOLIDADO (motor `avt_period`: inicial+compras−consumo=teórico, merma=teórico−real final; agrupable por Local/Almacén/Familia/Artículo; filtro de periodo; inicial estimado por apertura del ledger avisando; solo medibles suman = honestidad). Doc `docs/folvy_avt_diseno.md`. Cierra el frente 5 (inventario perpetuo cierra el AvT). (3) FRENTE ② NIVELES min/par = BASE DEL MRP II: tabla `stock_level` (5 campos; UI activa min+par, reorder/lead_time/safety listos para el nivel vivo futuro), RPC `stock_levels_overview`+`set_stock_level` (gestiona solo min/par sin pisar campos MRP), pestaña Niveles (bajo-mínimo arriba, edición inline) + en la ficha del artículo por local. (4) FICHAJE RESUELTO en dos capas: (a) MANUAL FIABLE del manager (`handleClock` escribe en BD vía `addClockEntry`, `source='manual'`+motivo obligatorio=rastro legal, sin GPS del manager ni bloqueo por horario); (b) DE RAÍZ: radio y modo de geofence CONFIGURABLES POR LOCAL (`clock_radius_m`, `clock_geofence_mode`; modo 'warn' deja fichar con GPS caprichoso marcando distancia → resuelve a Natacha sin manual). Migraciones `20260617T2000` (avt_period), `T2100` (stock_level), `T2200` (fix set_stock_level), `T2300` (geofence). Commits e829260, 1e21e53, 748db53, 7e86e25, 699dec1, b176bf3, 8cdb558 (todos rev-list 0). IDEA JULIO (anotada SIGUIENTE): NIVELES VIVOS por consumo (par=consumo medio×días entre pedidos; min=consumo×lead time+seguridad) sobre la tabla `stock_level` ya lista. OBLIGATORIOS A-C siguen abiertos. Antes (16/06): sesión de integraciones + estrategia de canal directo (Otter/HubRise diseño, tienda propia, motor de ofertas=NO clonar Pleez).)
> **Regla de oro**: el frente activo es el primero de "AHORA". Al cerrarlo, se mueve a "HECHO" y sube el siguiente.
> **Antes de abrir CUALQUIER frente: `conversation_search` del tema PRIMERO** (lección 08/06: el frente de modificadores ya estaba diseñado/benchmarkeado el 05/06 y se redescubrió a base de rodeos por no consultarlo; lección 09/06: la "capa 4" ya estaba construida y commiteada el 08/06 — el guion la marcaba como pendiente). Luego RECON contra fuente primaria (BBDD+repo+dumps) y **AUDITORÍA TSPOON** (`tspoon_dump/`), nunca contra este guion.

---

## AHORA (el frente activo y los 2-3 siguientes)

### ✅ HECHO (28/06) — Escandallos: B2 de raíz + saneo de unidades + duplicar receta
**(1) B2 DE RAÍZ (pantalla de revisión anti-duplicados al importar ficha).** El parche anterior (adopción de propuestas) NO bastaba: seguía duplicando ingredientes nuevos en producción. Raíz: la decisión del humano vivía solo en `mapping_proposal`, y cuando el Edge no crea la propuesta (índice único sin source_ref), el UPDATE de la decisión afecta a 0 filas SIN error → materialize no la encuentra → crea nuevo. **Solución:** la decisión se pasa DIRECTA a la RPC vía `p_decisions jsonb` y manda con prioridad absoluta; el modal construye el objeto completo y lo pasa; retirado el parche de adopción. Migración `20260628T1700_materialize_session_decisions.sql`. Verificado en vivo con la prueba de fuego (importar + REIMPORTAR el mismo plato, 0 duplicados). **(2) DUPLICAR RECETA** (pedido por Julio): RPC `duplicate_recipe_item` (copia plato + líneas + pasos + enlaces + foto, atómico, needs_review, folvy_code nuevo) + botón en el header del editor + título del plato editable (click en el nombre). Migración `20260628T1900_duplicate_recipe_item.sql`. **(3) UNIDADES NO CONVERTIBLES** saneadas en datos (26→0 líneas): hamburguesa 1ud=85g, gouda 1ud=20g, rulo 1ud=40g, Pasta Trufada base ud→g. Commits hasta a0b0ca7 (rev-list 0 0). DEUDA: regenerar `database.ts` (CLI roto → 2 casts puntuales: materialize, duplicate).

### 🔴 BLOQUEO POR UNIDAD NO CONVERTIBLE (frente de NÚCLEO — sesión dedicada, NO en caliente)
Una línea de escandallo cuya unidad NO es convertible a la unidad base del ingrediente hoy se SALTA EN SILENCIO (`explode_recipe_to_raws` hace `IF v_qb IS NULL THEN CONTINUE`): ni costea ni descuenta stock, y la venta se da por buena → **fuga silenciosa de stock / el AvT miente por omisión.** RECON HECHO de los 3 motores (`_qty_in_base`, `explode_recipe_to_raws`, `generate_sale_consumption`, `compute_sale_line_cost`). **Decisión Julio: BLOQUEAR, no avisar.** 6 PIEZAS aprobadas: (1) la línea no convertible nace `needs_review` "falta conversión", visible/bloqueante en el escandallo; (2) coste "incompleto", no 0€ disfrazado; (3) `generate_sale_consumption` registra "consumo pendiente/no medible" en vez de saltar en silencio (protege stock+AvT); (4) UX de corrección con retorno (aviso → ficha del ingrediente/unidades → de vuelta al escandallo); (5) prevención en la importación (la IA no crea líneas no convertibles en silencio); (6) saldar datos (ya hecho 28/06). **Toca `explode_recipe_to_raws`+`compute_sale_line_cost`+`generate_sale_consumption`** (de ellos dependen las 729 líneas de coste y TODO el inventario → máxima cautela, por capas). Disparador: antes de producción/7-sept. Principio asociado "unidades de uso amigables": la UI de conversiones debe dejar escribir "1 ud = 85 g" (legible) y calcular el qty_in_base por dentro (nadie teclea 0,0117 ni "25000 ml" por "1 garrafa de 25 L").

### 🔴 "ELIMINAR NO ELIMINA" (frente de NÚCLEO — sesión dedicada, reproducir antes de tocar)
Un plato que Julio confirmó borrar seguía vivo (`archived_at=NULL`, is_active sin tocar, 0 dependencias). RECON hecho: `kitchen_delete_or_archive_item` (DELETE físico si deletable, si no archiva) + `kitchen_item_delete_check` (deletable contra 8 tablas; guard `current_user_is_admin()/current_user_is_admin_or_manager_of()`). `confirmDelete` del editor NO traga el error. SÍNTOMA CONTRADICTORIO (confirmó, sin error, plato sigue, sin fecha de archivado) → NO diagnosticable sin REPRODUCIR con la consola abierta (ver qué devuelve la RPC: deleted/archived/error). **Siguiente paso: reproducir con DevTools; NO tocar la lógica de DELETE+CASCADE en caliente.** (La Ternera huérfana concreta se borró a mano el 28/06.)


### ✅ HECHO (25/06) — Reparto a Catcher (validado sandbox) + Hub público de Folvy Shop
**(A) CATCHER (reparto last-mile), completo y validado en sandbox.** Edge `catcher-dispatch` (auth + create order v1 a `staging-api.catcher.es`, envío real aceptado por Catcher con orderId) + `catcher-webhook` (estado/rider/coste real, cruza por externalId) + disparo automático `tg_auto_dispatch_catcher` (guardarraíl cuádruple: solo `source=folvy_shop` AND `accepted` AND sin carrier AND modo auto; frontera por secreto interno). Conector conectado en producción (local Alcalá). Folvy solo despacha donde Last NO lo hace. Fix colateral: activación de conectores tipo `credentials`. Commits adbb393/309387f/84be66c/933d05d/0f63c06 (rev-list 0). **DEUDA:** hoy solo dispara con `folvy_shop`; al entrar Llorente29 en HubRise → pasar a configurable por canal.
**(B) HUB PÚBLICO FOLVY SHOP (`/t/:slug`).** Escaparate multi-marca, diseño "fresco urbano" tipo Glovo modo claro (aprobado por Julio tras maquetas). RPC `shop_hub_by_slug` v3 + `ShopHubRoute.tsx` (hero con imagen real de portada + slogan configurable, franja info, chips de categoría, "lo más pedido", rejilla con **logos reales** + badges, carrito lateral). Slug de Llorente29 = **"foodint"**. Commit 2f6e17c (rev-list 0). **DEUDA (Julio cerró "muy a su pesar", a retomar — frente 🟠 abajo):** mucho dato es de MUESTRA (chips no filtran, "lo más pedido" fijo, valoraciones/categorías/badge Gratis inventados, carrito/buscar/entrar/dirección no funcionales); solo 8 de 17 marcas publicadas; layout no convence ("hemos ido para atrás").

### ✅ HECHO (27/06 noche-2) — FOLVY SHOP NÚCLEO TRANSACCIONAL: pago Stripe Connect + efectivo + caducidad + tablet/impresión verificadas
El Shop ya COBRA. **PAGO STRIPE CONNECT** completo y probado en vivo (tarjeta 4242): direct charges sobre la cuenta conectada del restaurante, `application_fee` para Folvy, Edge `shop-payment-intent`+`stripe-webhook` (firma verificada, confirma server-side), Payment Element en checkout, onboarding real desde panel admin (Account Links) + comisión sin SQL. **MÉTODOS DE PAGO configurables por cuenta** (online/efectivo recogida/efectivo entrega); efectivo nace ACEPTADO (dispara impresión+Catcher), online espera webhook; feed/kds ocultan folvy_shop en 'new'. **CADUCIDAD** de abandonados (cron 5min). **INGESTA** del pedido folvy_shop→KDS+stock+AvT+Catcher (vía `place_shop_order`+`adapt_folvy_shop_order`, ya operativa). **TABLET+IMPRESIÓN verificadas con hardware real** (Tablet J Plaza Castilla, NT311, 3 tickets). Fix selector de locales (timing). Commits e2de5bc/baf9b08/75ea725/638ed8d (+ pago previos b117c8b/ce5bc5e/1254de3), rev-list 0. **DEUDAS PRODUCCIÓN STRIPE:** Bizum + quitar iDEAL/Klarna en la conectada; plataforma DEFINITIVA Folvy SL (no Fplvy test); webhook de producción; comisión por defecto para clientes nuevos; ROTAR secrets de test.

### 🔴 ENLAZADO CATÁLOGO ↔ ESCANDALLO (frente transversal — desbloquea el AvT de TODAS las ventas)
**Hallazgo del 27/06:** el motor de consumo de stock (`generate_sale_consumption` + `tg_sale_consumption_on_complete`) está construido y es CORRECTO y AGNÓSTICO de source (cualquier venta descuenta stock al pasar a `completed` vía escandallo, idempotente). PERO está **bloqueado por DATOS**: los 107 `menu_item` con `recipe_item_id` apuntan a recipe_items VACÍOS; los escandallos reales (ej. Birria Chicken Bowl 19 líneas, recipe_item `912d49c4`, `menu_items_enlazados=0`) existen en Kitchen SIN conexión con el catálogo de venta. **Sin este enlace, NINGUNA venta (Shop/Glovo/Uber/Last) descuenta stock ni alimenta el AvT** — no es problema del Shop, es transversal a todos los canales. Patrón a resolver: RECON de cómo quedó el enlace tras la importación de Last (por qué apuntan a recipe_items vacíos), emparejar por nombre/código los 107 productos con sus recipe_items reales, repoblar `menu_item.recipe_item_id`, verificar con una venta completada que descuenta. Encaja con el frente 1 (cobertura de escandallos) y con la carga de costes CTB. **Disparador:** antes de fiar el AvT con ventas reales.

### ⚪ FOLVY SHOP — notificación al cliente (arquitectura diseñada, APARCADA por dependencia externa)
Diseño agnóstico de canal aprobado: tabla outbox `customer_notification` (sale_id, customer_phone/email, event, payload, channel, status, attempts) + dispatcher con adaptadores (email Resend = 1er adaptador real; WhatsApp/SMS enchufables) + preferencia de canal por cuenta. **NO construir hasta que Julio resuelva el proveedor de WhatsApp** (cuenta Meta Business + BSP + plantilla utility aprobada). Costes (España 2026): WhatsApp utility 0,0166€/msg (gratis en ventana servicio 24h) + ~49€/mes BSP 360dialog (sin markup, el más limpio); SMS A2P 0,05-0,09€/msg (el más caro, NO usa la tarifa personal — proxy/centralita); email casi gratis. Modelo A (un WhatsApp de Folvy) vs B (por restaurante, tipo Stripe) sin decidir. Sirve transversalmente (Shop, reservas, fidelización, avisos internos, pedido a proveedor).

### 🟠 FOLVY SHOP — CRM + ofertas propias + fidelización + marketing (roadmap grande, sesión dedicada)
**Hallazgo clave del 27/06:** el teléfono de los pedidos `lastapp` es un PROXY de plataforma (Uber/JustEat usan número-centralita único + código; "Влад С." con 260 pedidos/13 marcas/6.487€ = ruido técnico, NO un cliente). El CRM real solo se construye desde canales PROPIOS (Folvy Shop, reparto propio real) hacia adelante — hoy es ínfimo (Shop recién nacido). PERO direcciones/horas de TODOS los pedidos SÍ son explotables para **BI geográfico agregado** (zonas calientes, horas pico por marca) — esto es LEGAL (estadística interna) y de alto valor. RGPD: usar dato de plataforma para CONTACTAR sin consentimiento es ilegal. Idea Julio (owned-channel migration, lo hacen los mejores): detalle físico al entregar (pegatina/QR) que invita a registrarse → puente de consentimiento → marketing legal. **Tres patas a ordenar:** (1) BI agregado (ya, legal), (2) máquina de captación de consentimiento (Shop captura cliente+consentimiento; detalle físico convierte anónimos), (3) CRM+segmentación+loyalty+ofertas dirigidas+marketing (RRSS/WhatsApp, sobre la capa de notificaciones). BENCHMARK iniciado: Punchh/Paytronix/Thanx (loyalty card-linked frictionless + recompensa consciente del margen = terreno Folvy), Talon.One (motor ÚNICO de reglas ofertas+loyalty), Eat App (WhatsApp en loyalty suite). **Decisión Pleez (ofertas a PLATAFORMAS Glovo/Uber, NO clonar) sigue vigente** y es DISTINTA de ofertas en el Shop PROPIO (territorio limpio, sin incumbente, con guardarraíl de margen real). Empezar con estudio de mercado dedicado + diseño aprobado ANTES de construir (ritual deuda 0). Julio quiere "lo mejor de todos" + capa de marketing propia.

### 🟠 FOLVY SHOP — MIGRAR todo a estilo B + datos reales del Hub (frente de diseño, tras unos días de uso)
Julio eligió el **estilo B** (moderno tipo Glovo/Uber 2026, blanco+aire, coral solo como acento) para el checkout y dijo que "posiblemente se cambie todo a ese estilo". Lo usa unos días; si confirma, frente propio: (1) **sistema de diseño B** (tokens: color, tipografía, radios, espaciado — hoy cada superficie lleva los colores a mano), una vez; (2) migrar las superficies existentes (Hub, carta de marca, modal de config, panel de carrito, mini-panel) con **maqueta por superficie antes de tocar** (ya viste Hub y checkout en B). La LÓGICA no se toca, solo la piel. (3) Datos reales del Hub que seguían de muestra: chips de cocina que filtren (campo tipo de cocina EDITABLE por marca, lo rellena el cliente), "lo más pedido" ya es dato real de ventas; badge "Gratis"/zona desde delivery_zone real. Publicar las 17 marcas (hoy 8). FRENTES FUTUROS derivados: reseñas propias del cliente final; "te faltan X€ envío gratis" (se activa al tener zona en checkout).

### ✅ HECHO (24/06) — Bug silencioso de recepciones RESUELTO + modal "Meter al stock"
Recepciones que confirmaban sin meter stock en silencio (raíz: goods_receipt_line sin red de seguridad). Construido: post_pending_receipt_line + post_pending_receipt v3 + modal PostPendingModal que resuelve cada pendiente SIN SALIR (casar artículo / montar formato con enlace auto al proveedor del albarán) + badge rojo fuerte + fix crítico de needs_review (cuenta toda línea sin movimiento, el indicador ya no miente) + unidades de longitud cm/m. Commits 0fa9993, a275291, 1a55c5b, fb7f37f. Validado en vivo (ALB-00014 3 líneas dentro, alubia 18.000g). DEUDA DE RAÍZ futura: estado "confirmada con pendientes" en el momento de confirmar (sesión dedicada, benchmark blind-receiving).

### 🔴 CARGA DE COSTES LLORENTE29 — fuente CTB (frente activo, arrancado 23/06)
Sin coste real, el food cost está ciego en buena parte de la carta. Fuente de verdad = informe **Compras y Ventas de Cloudtown** (lo que CTB paga de verdad), NO tspoon (coste teórico con datos rotos). Hecho hoy: 5 meses de PDF (ene–may) cruzados contra los 150 productos de Folvy → `Folvy_vs_CTB.xlsx`. **AHORA mismo:** revisar y CARGAR los ~14 ingredientes sin coste que CTB sí tiene (tabla de revisión generada, NO cargada aún — proteger de errores ×1000 en conversión Kg→g/Lt→ml). Luego: (a) packaging y herramientas mismo cruce; (b) **sub-recetas** que cocinan (Arroz criollo, pico de gallo, salsas Chilli/Verde/Smokey) = van por ESCANDALLO, no por precio; (c) corregir dato roto "Milanesa Ternera Rebozado 233,96€" → real 3,63€/ud (Excel) ó 14,62€/kg (CTB). Cargar precios "del motor" (last_purchase/average_weighted), no fixed, para que las facturas los actualicen. Ficheros: el Excel ya generado + RPC/UI de carga de precios de ingrediente. **FEATURE confirmada (caso de uso real): ALARMA DE PRECIOS** cuando una recepción supera el coste de referencia de CTB, aunque la venta venga de marca cedida — el coste CTB = coste estándar; recepción más cara = avisar.

### 🟠 BUG cliente 2 — pantalla "marcas pendientes" mezcla marcas de otra cuenta
La pantalla de vinculación de marcas del cliente 2 muestra marcas PROPIAS de Llorente29 (Foodint) porque el importador/listado pide el catálogo **por organización** (Cloudtown, compartida) en vez de **por `locationId`** de la cuenta. Confirmado contra la API de Last (`/catalogs?locationId=…` aísla; `/organizations/{org}/catalog` mezcla). NO afecta a ventas (el webhook resuelve por tienda → cuenta, verificado). **RECON:** `scripts/import-last-catalog.mjs` y el endpoint que llena la pantalla — cambiar a pedir por las ubicaciones de la cuenta. NO importar catálogo del cliente 2 hasta el fix. Mismo patrón que el bug de combos ya cazado.

### ⚪ Onboarding Last (cedidas) — manual borrador + cerrar acceso autorizado
Manual `docs/folvy_onboarding_cliente_last_cedidas.md` (borrador): proceso de alta de un cliente nuevo con marcas cedidas (invitar correo desde la org del cedente → instalar integración Folvy en cada tienda → vincular tienda↔local en Folvy). DOS pasos marcados ⚠️ pendientes de cerrar el ACCESO AUTORIZADO con Last/CTB (NO usar el atajo de manipular la URL: frágil + riesgo de acuerdo/datos). Versionar el manual en `docs/` con Code cuando el acceso esté claro.


### 🟠 IMPRESIÓN — versionar (deuda crítica) + afinar layout 3 tickets + botón reimpresión
La impresión automática FUNCIONA en vivo (pedido aceptado → sale por la NT311 por LAN sin nadie con Folvy abierto). Pasos:
- **ANTES DE NADA (deuda crítica del 21/06):** VERSIONAR en `supabase/migrations/` TODO el SQL de impresión que vive solo en BD (modelo `printer`/`print_job` + RLS, `claim_print_jobs`/`report_print_job`/`enqueue_print_job`, `order_for_print`, trigger `tg_auto_print_on_accept`) **y las RPC by-token de la Estación de Tablet** (`set_product_availability_by_token`, `availability_panel_by_token`, `search_products_by_token`, `preview_scope_by_token`, `device_location_by_token`, `orders_feed_by_token`, `set_order_status_by_token`). METER el AGENTE en el repo (hoy en `C:\folvy-print-agent`, fuera): decidir carpeta `print-agent/` o repo aparte. Regenerar `src/types/database.ts`.
- **AFINAR LAYOUT — BOLSA/FACTURA RESUELTA (24/06, motor de imagen, EN VIVO):** la factura/bolsa ya es CLON de Last (imagen canvas → ráster), validada en papel con Milanesa House. Logo protagonista sin deformar, €+acentos perfectos, dirección desglosada, IVA, descuento tachado, QR, pie Folvy. Ficheros del agente: `ticketImage.js` (nuevo, render canvas), `folvy-print-agent.js` v3, `DejaVuSans.ttf`/`-Bold.ttf`, `folvy_pie.png`; SQL `order_for_print`+`fiscal_for_print`+`fill_line_discounts`+columnas de descuento. **PENDIENTE de este sub-frente:** pasar COCINA y PEGATINAS al mismo motor de imagen (hoy siguen por texto ESC/POS `renderForType`, salen pequeños/sin alérgenos en pegatina).
- **BOTÓN DE REIMPRESIÓN/MANUAL a voluntad** (imprescindible: papel atascado, copia extra, pedido antiguo): encola con `enqueue_print_job` o un job `by_order` con `source='reprint'/'manual'`. Ficheros: `OrderCard.tsx`, `OrdersFeed.tsx`, `ordersFeedService.ts`.
- **CAPA 2 (frente aparte, después):** iconos gráficos (moto, alérgenos, logo) = bitmap ESC/POS desde PNG. Sunmi partner solicitada (revisión) para el transporte cloud (impresora tira sola, ideal multi-cliente; `escpos_network` con agente es el camino de hoy).

### ⚠️ OBLIGATORIOS abiertos hoy (12/06) — cerrar antes de avanzar otros frentes

**A. 🔴 ACCESO TRABAJADOR — reentrada completa (PIN).** Mínimo aplicado y subido (worker puro sin botón "salir"; su acceso es QR de UN SOLO USO y `signOut` lo dejaba atrapado pidiendo email/contraseña que no tiene). Completo OBLIGATORIO (doc `docs/OBLIGATORIO_acceso_trabajador_reentrada.md`): "salir" → `LoginEmpleado` (nombre+PIN), NUNCA login de gestor; habilita tablet común futura (recetas/recepciones/pedidos, no fichar); requiere PIN por empleado. **ACCIÓN INMEDIATA: reenviar el QR a trabajadores ya atrapados (Johanny).**

**B. 🟡 PWA — botón "Instalar" debe instalar directo, no mostrar instrucciones.** En Android el `beforeinstallprompt` no se disparó → cae al modal de instrucciones (complicado para gente mayor). El botón ya está en HomeEmpleado; falta que salte el prompt nativo. Diagnosticar (Application→Manifest en Chrome escritorio; por qué Chrome Android no marca instalable).

**C. 🔴 COMPLETADO MASIVO — no retira `needs_review` por `review_notes`.** Diagnosticado, no arreglado: `applyEnrichment` solo cierra si `hasFamily && vatDerived && !hasIncident`, y `hasIncident = review_notes != null`. Los ingredientes que crea la función estrella llevan `review_notes` ("completar coste/proveedor/formato") → nunca cierran. DECISIÓN: distinguir "sin clasificar" (familia/IVA, badge se va) de "sin coste real" (precio, badge queda). Arreglar en `recipeAiService.ts`.

**D. ✅ EDITAR PRECIOS DE PROVEEDOR desde la app — RESUELTO (16/06):** cerrado de raíz por la reconstrucción a €/base — `last_price` es ahora €/UNIDAD BASE canónico, las secciones `SupplierItemsSection`/`PurchaseSourcesSection` editan base-first en €/kg (€/L, €/ud) y el caso Delicias-COHELDI queda corregido POR CONSTRUCCIÓN (ver HECHO «motor de aprovisionamiento €/base»). Contexto histórico del bug: Las secciones `SupplierItemsSection.tsx` (tiene input de precio + `savePrice` + `updateArticleSupplier(link.id, {lastPrice})`) y `PurchaseSourcesSection.tsx` EXISTEN pero están muertas/no operativas en la ficha del ingrediente. Sin esto Pamela NO puede mantener precios y depende de UPDATEs por SQL (no operativo). **Decisión de raíz pendiente ANTES de reparar: qué representa `last_price` canónicamente — el módulo de PEDIDOS lo usa como €/CAJA (`cantidad × last_price`) mientras Kitchen lo usa como €/BASE (`last_price / qty_in_base`); esa ambigüedad es la causa.** Caso testigo: Delicias de Pollo Southern–COHELDI tiene `last_price=8,99` (Julio confirma = €/kg) cargado donde el pedido lo cobra como €/caja → en el PDF salió "8 cajas × 8,99 = 71,92 €" cuando la caja de 2,2 kg debería ser 19,78 €/caja. Reparar la sección + corregir Delicias desde la app ya reparada (NO por SQL). RECON: por qué las secciones no funcionan hoy.


### 0. ⏳ CHECKPOINT (10/06): validar la RECEPCIÓN espejo del albarán con varios albaranes
Construida la noche del 9/06 (espejo del albarán + árbol de formatos `ensurePackTree` + tarjeta rediseñada con jerarquía: nombre protagonista, "el albarán dice" agrupado, formato en una línea con pack real, recibido a ciegas con "→ X al almacén", foto del albarán a la izquierda, rojo+motivo si no cuadra). Commits `5230ff4..62a225e`, build verde. **Julio no está del todo convencido y la probará recepcionando varios albaranes reales en Folvy Interno.** Validar: (a) la tarjeta se lee clara; (b) al "ajustar como el albarán", `ensurePackTree` crea el nodo interior contable + la Caja (`qty_per_parent`, `qty_in_base` derivado) sin mover coste; (c) "→ X al almacén" refleja lo que entra. **Si cuadra → recepción a HECHO; si no → ajustar tarjeta/parser/árbol.** Solo entonces seguir con el frente 1.

### 0.bis 🔴 DELIVERY PROPIO + INGESTA CANÓNICA (decisión estratégica mayor del 10/06)
**Dos caras del mismo frente** (diseño cerrado, construcción pendiente; docs `folvy_ingesta_canonica_diseno.md` + `folvy_estrategia_delivery.md`):
- **El porqué (Julio, urgente, no a medio plazo):** Llorente29 quiere DEJAR Last (>600€ solo por integración, caro); riesgo competitivo real de que otro SaaS ofrezca "delivery integrado incluido" y se lleve al cliente. La integración propia de delivery es DEFENSA a corto plazo + argumento de venta + retención ("todo en Folvy = difícil que se vaya"). Glovo es INNEGOCIABLE (>50% de pedidos en España).
- **La estrategia (research a fondo 10/06):** el mercado obliga a elegir — quien tiene Glovo España compite contigo (Deliverect, GrubTech, Ordatic-bajo-GrubTech) o no lo tiene (HubRise, KitchenHub). **Otter rompe el patrón**: tiene Glovo España (verificado con pantalla de conexión real), NO compite (Otter=delivery, Folvy=cocina/coste/MRP), y tiene API de partner completa (Orders/Menus/Finance/Reports) camuflable bajo Folvy. **Correo de partnership ENVIADO** (esperando respuesta: modelo reseller white-label, economía, acceso API a cuentas, cobertura ES). Glovo directo (DH API) en cola sin fecha como destino a largo plazo. HubRise (Uber+JustEat, sin Glovo ES) como alternativa parcial.
- **La arquitectura (cómo encaja):** cualquier proveedor (Otter, Last, Glovo directo) entra como ADAPTADOR (`external_source`) sobre el núcleo canónico. **Folvy es la verdad del catálogo** (propias: Folvy manda; cedidas: Folvy espeja sin tocar pero costea). Casado por id estable (`organizationProductId`); lo no reconocido → cola de excepciones = 100% de fiabilidad. Catálogo vivo vía `catalog:updated`. El RECON demostró que el 70% ya existe (`menu_item.external_id/external_source`, `sale_line`, `recipe_item`↔`menu_item`↔`override`).
- **Próximo movimiento (act. 16/06):** la **VÍA RÁPIDA es HubRise vía el CLIENTE 2** (1 local, 6 marcas, Uber+JustEat, SIN Glovo) — desbloquea HubRise sin esperar a Otter; correo de reactivación a Janaina preparado; build = adaptador `hubrise` (1 API cubre Uber+JE) sobre ingesta canónica. **OTTER:** esqueleto listo hasta el alta (`docs/folvy_adaptador_otter_diseno.md` + `supabase/functions/otter-webhook`: frontera HMAC-SHA256, catálogo bidireccional, sin API de promos, deploy `--no-verify-jwt`), BLOQUEADO esperando Application ID + Client Secret de un Account Representative (2º correo de partnership enviado). Glovo de Llorente29 sigue dependiendo de Otter/HubRise. **Hallazgo LAST:** no expone abierto/cerrado de marca/canal → la alarma de disponibilidad se construye sobre HORARIOS, no sobre Last. En paralelo: matrículas externas faltantes (87 `menu_items` sin `external_id`) + cola de excepciones.

### 0.ter 🟠 MÓDULO FOLVY ORDERS — EN CIERRE (FRENTE ACTIVO, act. 19/06)
**CIERRE MAYOR el 19/06 (5 piezas en producción, validadas en vivo con Glovo).** El módulo "Folvy Orders" (fusión Pedidos `/orders` + Cocina `/orders/cocina` + Ajustes) tiene: feed operativo multi-canal, ruta completa, escandallo al pulsar (= Cook Mode del KDS), marcar línea sincronizada con el KDS (`kds_line_state`), **puente KDS→feed con la ARQUITECTURA DEFINITIVA DEL EMPUJE (Opción A): mover `order_status` → trigger `trg_sale_push_status` → `net.http_post` fire-and-forget a Edge `order-advance` → Last → Glovo; FUNCIONA DESDE EL KIOSCO DE COCINA sin usuario**, y ciclo de vida por tipo de reparto (plataforma cierra al recoger el rider y NO empuja el cierre = evita `INVALID_STATUS_CHANGE`; pickup/propio con sus botones). Commits ba89312/d70dae4/63d2c54/87c1a4e/b885e48/0492702/ae88d20/0f6cce8/6c1d80d. **NO entra en producción hasta el 100% (Julio).** CERRADO: #1 ruta+empuje, #2 escandallo, #3 marcar línea, #7 puente, 7a ciclo de vida. **PENDIENTE para el 100%:** #4 cierre anti-faltantes (marcar todo→cerrar→dispara impresión) [CERRADO 19/06: aviso no bloqueante al cerrar con líneas sin marcar], #5 impresión [PRIMERA FASE HECHA 19/06: ver frente 0.quater abajo — renderizador capa 1 en producción; falta código de plataforma + capa 2 imágenes + transporte Sunmi], #6 modificadores con flag real [CERRADO 19/06: `orders_feed` expone group_type real del catálogo, color por tipo; heurística de texto solo cuando el grupo no está poblado], #8/9 cruce estaciones + collection LIKE, **7b REPARTO PROPIO** (flota Catcher OBLIGATORIO/Jelp/Shipday + estado "En ruta" + métricas de tiempos = frente mayor). Post-100%: kiosco del feed (puerta B), alinear KDS al navy, alinear la secuencia de estados con Last (evitar 400s), observabilidad del empuje async (log de fallos). **Frente aparte apuntado:** migración de escandallos cedidos Folvy Interno→Llorente29 (entre cuentas, sesión dedicada, máxima cautela). **DECISIONES:** UNA tablet Folvy para propias+cedidas; escandallo de cedidas obligatorio (Folvy controla su stock+pedidos); toggle de empuje por organización (hoy activo en Foodint+Cloudtown).


### 0.quater 🟠 IMPRESIÓN DE TICKETS — FRENTE ACTIVO (primera fase en producción, act. 19/06 noche)
**Capa agnóstica ESC/POS + adaptadores de transporte** (mismo patrón que TPV/canales). Diseño aprobado `docs/folvy_impresion_diseno.md`. **3 documentos** (maquetas validadas con datos reales): bolsa/cliente (logo de marca + fiscal + entrega + económico CON precios + IVA + QR a la shop centrado con caption configurable), cocina (agrupado por categoría, SIN precios, SIN alérgenos — los alérgenos van al Cook Mode al pulsar), pegatina opción (c) (una por artículo de comida + una agrupada "bolsa bebidas/postres"). **HECHO (capa 1, en producción):** D1 logo por marca, D2 shop_url+qr_caption por marca, D3 iconos UE (14 PNG, icono+texto cumple ley), `orders_feed` ampliado (cabecera de marca + `family` vía **`recipe_family`** + combo_item con su familia), renderizador `ticketRenderer.ts` (modelo `TicketBlock` → 3 docs texto+QR; `renderLabels` aplana combos en artículos físicos y expande qty; bebidas por familia a su bolsa), preview `TicketPreviewModal.tsx` (papel térmico 80mm en pantalla, QR vía lib `qrcode`), botón impresora en `OrderCard`. Validado en vivo (combo DÚO Meraki Pita=4 pegatinas correctas, multiplicador 2x pita=2 pegatinas). Commits cc9a944/963ffbd/30f2e49/59dc419. **AHORA SIGUIENTE (lo primero al retomar):** **CÓDIGO DE PLATAFORMA REAL** — el código grande del ticket (#XXXXX) es un RECORTE de UUID, NO el G406 de Glovo / código largo de Uber-JE que el rider pide. Está enterrado en `raw_tab` sin extraer. RECON: `select jsonb_object_keys(raw_tab::jsonb)` del pedido para localizar la clave (candidatos displayId/friendlyId/orderCode/pickupCode o dentro de delivery), luego extraer en el adaptador `lastapp-webhook` a columna nueva `sale.platform_order_code` + exponer en `orders_feed` + usar en el ticket. OJO: G406 es formato Glovo, Uber/JE usan códigos largos distintos (no uniforme entre plataformas). **DESPUÉS:** capa 2 = imágenes (logo de marca + iconos de alérgeno rasterizados a bitmap ESC/POS, hoy en texto); modelo `printer`/`print_job` (cola + config); **adaptador SUNMI NT311 Cloud Partner** (transporte decidido: impresora cloud tira de Folvy vía MQTT+HTTPS; requiere registro en partner.sunmi.com, APP_ID/APP_KEY, activar región EU, exponer URL de Folvy, vincular SN — tiene fricción real, sesión dedicada, Claude guía paso a paso); disparadores (al servir sobre kds_ticket_station_state + manual + reimpresión).
---
_Contexto histórico (diseño fase 1/2):_ **Fase 1 estuvo en producción y validada en vivo** (#RP65P received→accepted): `sale.order_status` + webhook espeja + Edge `hubrise-order-status` empuja a HubRise. **Fase 2 = la consola operable** (diseño en papel hecho y commiteado: `docs/folvy_pedidos_pestana_diseno.md`): feed (cuadrícula/lista + filtro por estado + semáforo de tiempo, en tiempo real), detalle con contenido completo (líneas + modificadores + combos + **alérgenos** de escandallo), y **acciones del ciclo de vida** (aceptar/rechazar/listo/ajustar hora) que llaman al Edge, con **reglas por plataforma** (Uber auto-cancela <10 min y soporta auto-aceptación; Glovo no cancela tras aceptar; JustEat mínimo=accepted) y **auto-aceptación configurable por canal/marca** (guardarraíl horario+stock). **Antes de construir**: (a) responder las 4 decisiones del §9 del diseño (sobre todo si el primer cierre es "feed + aceptar/rechazar" o todo de una); (b) RECON del §8 (dónde montar la pestaña, feed/lista reusable, realtime, channelBadge, render de líneas, permisos). **NO depende de P-A.** Declarado para fases siguientes: pausar local (fase 3), 86 por producto (fase 4), Catcher, impresión, cerrar por marca/canal (P-A).

### 1. 🔴 Cobertura de escandallos (food cost ciego en buena parte de la carta)
El diagnóstico del 09/06 (al cerrar el recosteo) lo destapó con datos duros sobre las 349 líneas product de Folvy Interno: solo **186 tienen coste**. El resto es food cost ciego, repartido en:
- **138 líneas sin escandallo** (118 casadas a plato sin receta con coste + 20 `no_recipe`): platos cascarón. Falta **escandallo** (Kitchen / Pamela).
- **17 líneas de reventa sin precio de compra** (Fanta, Agua 50cl, Tarta 3 Leches…): el `recipe_item` está clasificado como raw vendible, pero `fixed_cost` NULL → coste 0 correcto pero ciego. Falta **cargar el precio de compra** (ficha de ingrediente / OCR factura).
- **2 líneas** con menu_item sin recipe_item: falta **mapeo**.
- (6 combos con coste >0 y receta-padre sin coste = correcto, coste por componentes. No tocar.)
- **Por qué lidera**: el motor de coste, el consumo teórico (ya vivo) y el AvT están sanos, pero solo dan la verdad sobre el 53% del dinero con receta. Subir cobertura es lo que hace el food cost de Llorente29 REAL, no parcial. Es trabajo mixto: escandallos (datos, Pamela) + precios de reventa (datos) + algún mapeo. **Antes de abrir: decidir si es un frente de PRODUCTO (pantalla que lista "platos vendidos sin escandallo" por € y guía a crearlos) o de DATOS (poblar a mano). La pantalla de excepciones (`SalesExceptionsPage`) ya muestra parte de esto.**

### 2. 🟢 Limpieza de catálogo (eliminar/fusionar proveedores e ingredientes)
611 ingredientes muertos, proveedores duplicados/[Copia]. Dolor masivo, producto para cualquier cliente. RECON propio antes de tocar.

### 3. 🟠 Entrega B de la pantalla de excepciones — ACCIONES de resolver (verificar si está hecha)
La Entrega A (señal + vista read-only de excepciones) está commiteada. La Entrega B (botones link/ignore/delist + clasificar producto: resale/dish/combo) tiene las RPC en BBDD (`resolve_unmapped_sales`, `classify_unmapped_product`) — **pero hay que verificar si la UI las cablea o `SalesExceptionsPage` sigue read-only**. Si está read-only, este es el remate: conectar las acciones. Encaja con el frente 1 (resolver ciegos es subir cobertura).
> **Nota 09/06**: tras jubilar la identidad del recast, esas dos RPC siguen llamando a `recast_lastapp_sales` (que ahora recasa por el canónico vía `reprocess_sale`). Funciona, pero su patrón "recasa toda la cuenta" es más caro de lo necesario; cuando se toque, valorar que re-adapten solo las ventas del producto resuelto (deuda menor, no urgente).

---

## SIGUIENTE (cuando se libere lo de AHORA)

### 4. 🔴 Capas 5-7 del subsistema de fiabilidad (impacto + alarmas)
La señal central (capa 4) está VIVA y verificada. Faltan las capas que actúan sobre ella, y dependen de inventario perpetuo cerrado:
- **Capa 5 — impacto en stock**: merma fantasma calculable (las `no_menu_item`, hoy 0 €) + consumo desconocido (las `no_recipe`). Diseño en `docs/folvy_fiabilidad_casado_diseno.md`.
- **Capas 6-7 — alarmas y avisos**: producto nuevo vendiéndose sin receta; % ciego sobre umbral (campana manager + email); avisos en inventario y compras proporcionales a `pedido.origin` (manual no alarma; To-Par/MRP sí).
- **3 decisiones abiertas**: umbral ventas-ciegas (configurable vs fijo); alarma producto-nuevo (tiempo real vs cierre de servicio); impacto en stock (€ vs % merma).

### 5. ✅ HECHO (17/06) Inventario perpetuo capa 2 — conteo real cierra el AvT
**CERRADO.** El AvT (Teórico vs Real) está vivo en sus dos formas: puntual (último conteo) y por periodo consolidado (motor `avt_period`, agrupable por Local/Almacén/Familia/Artículo, con salud del dato). El consumo teórico (ventas×escandallo) ya estaba; ahora se cruza con el conteo real y materializa la merma. Diferenciador cumplido: nadie en hostelería cierra este bucle con la honestidad del dato (solo medibles suman; inicial estimado avisa). Ver HECHO «MÓDULO ALMACÉN + AvT». **Nuevo SIGUIENTE derivado: NIVELES VIVOS por consumo (frente 5.bis abajo).**

### 5.bis 🟠 NIVELES VIVOS por consumo (MRP II — idea de Julio 17/06)
La base manual ya existe (frente ② cerrado hoy: tabla `stock_level` con min/par + reorder_point/lead_time_days/safety_qty LISTOS sin usar). El nivel vivo lo CALCULA el sistema: **PAR = consumo medio diario × días entre pedidos**; **MÍNIMO = consumo medio diario × lead time del proveedor + stock de seguridad**; punto de pedido = mínimo. El consumo medio ya está (motor consumo del AvT). Faltan dos datos fiables: **frecuencia de pedido** por artículo/proveedor y **lead time** del proveedor. "IA propone, humano decide": el sistema sugiere el nivel, el usuario lo acepta o lo pisa. Deuda 0: no encender el cálculo hasta que esos dos datos sean sólidos. Disparador: datos de compras estables + lead time por proveedor.

### 6. 🟢 Sidebar "Modificadores por revisar" (repaso global G3)
Lista todos los modificadores sin impacto de todos los platos, ordenados por dinero, + "sugerir para todo". Reutiliza la tarjeta de la pestaña. Disparador: cuando haya volumen real (escandallos poblados). La pestaña por plato YA está (uso contextual); esto es el barrido global.

### 6.bis 🟠 KDS NIVEL 2 — impacto del modificador en RECETA y COSTE (diferenciador)
El KDS Capa 1 está completo (tablero+ticket completo en producción, 13/06). El Nivel 2 es el remate que golea a tspoon/R365: que un modificador altere el escandallo y el coste AL VUELO. La tabla `modifier_recipe_impact` YA existe (modelada, 3 filas) y G3 (pestaña por plato, Edge `propose-modifier-impacts`) ya estaba hecho del 08/06 — **conversation_search obligatorio antes de abrir** (puede haber más construido de lo que parece). Construir = poblar el mapeo de los 221 modificadores `line_type='modifier'` → su impacto (IA propone, Pamela confirma) y mostrar el coste/margen real ponderado por mix vendido. Diseño en `docs/folvy_kds_ticket_completo_diseno.md`. **IDEAS JULIO Capa 2 KDS:** opciones de visualización del ticket (cuadrícula/lista/seguidos); sonidos configurables; Cook Mode dedicado de combos; realce extra al entrar ticket con nota.

### 7. 🟢 Unidades de uso amigables + renombrar formatos confusos ("Uni" → "Bote 200 g")
Gestos de cocina ("1 papel", "1 loncha", "1 cazo"); el cocinero nunca escribe gramos. Cada artículo define SUS unidades de uso (etiqueta+factor) sobre `recipe_item_unit_conversion`. RECON propio.

### 8. 🔴 Migración Llorente29 (poblar la cuenta real desde Folvy Interno)
Paso físico hacia producción. Todo el trabajo vive en Folvy Interno (00000000-…-0001); Llorente29 (51ad1792-…) vacío hasta migrar.

### 9. 🟠 Pulido de demo
Responsive/móvil (permiso App.tsx), www.folvy.app DNS, editar perfil propio.

### 10. 🟠 TIENDA PROPIA — Folvy Shop (canal directo sin comisión)
Estudio cerrado (`docs/folvy_tienda_propia_estudio.md`) + maquetas hechas (storefront de marca con modificadores + hub multimarca con carrito cruzado). Único canal directo que CONOCE el margen real (escandallo + economía de plataformas). El pedido entra por la ingesta canónica (`external_source='folvy_shop'`) → KDS + stock + AvT (cierra el bucle). Multimarca nativo + **carrito CRUZADO entre marcas con UNA entrega** (ventaja dark-kitchen que Glovo no da). Stripe Connect MVP. Fases: S1 pickup → S2 Uber Direct → S3 margen+promos (Ley Ómnibus) → S4 CRM → S5 multimarca/dominio/SEO. **MARKETPLACE B2C de Folvy (agregar a todos los clientes) = OTRO negocio, APARCADO** (compite con los partners, problema de tráfico/CAC, contradice el sin-comisión). Disparador: estabilizar Llorente29 en producción, o para el cliente 2.

### Decisión 16/06 — MOTOR DE OFERTAS POR PLATAFORMA = NO clonar (Pleez ya existe)
La idea de Julio (ofertas por canal + clima + deporte + rentabilidad/margen) **ya existe en producción**: **Pleez** (trypleez.com, Madrid, 2020, Buenavista Equity) hace push de 1 clic a Uber Eats/Glovo/Deliveroo con guardarraíles de margen, disparadores por reglas, **inputs de clima y eventos deportivos** y competitor tracker (opera vía credenciales del restaurante + scraping de escaparates). **Sapaad** = promos margen-multicanal en tiempo real; **Nory** = clima+eventos para forecasting. **DECISIÓN deuda-0: NO construir el clon** (océano rojo, incumbente local financiado). Folvy se queda SOLO con el **guardarraíl de margen real por plato×plataforma** (sale gratis de la Capa A/C de economía de plataformas) — lo único que Pleez no calcula bien porque no tiene tu escandallo. Pleez = posible integración, no enemigo. Diseño + benchmark honesto en `docs/folvy_motor_ofertas_diseno.md`. Disparador del guardarraíl: cerrar EP1/EP2 de economía de plataformas.

### 11. 🟢 Bloque 6c — Vista «proveedores que se encarecen» (tendencias de precio)
**El frente de aprovisionamiento/coste quedó mayormente CERRADO (16/06): motor €/UNIDAD BASE + tres alarmas de precio en recepción (puntual/pactado/deriva) en producción (ver HECHO «motor €/base»).** Esta es la SIGUIENTE pieza: panel que corre `price_drift_for` sobre TODOS los artículos de la cuenta, ordenado por **€ EVAPORADO vs la mediana del periodo** (decisión tomada: ordenar por DINERO evaporado, no por %), clicable a la ficha del ingrediente/proveedor. Es la cara AGREGADA de la 3ª alarma de recepción (deriva), que ya vive línea a línea. Diseñado, NO construido: requiere función SQL `price_drift_all` nueva (hoy `price_drift_for` es por artículo) + UI. Se hará sobre datos limpios (tras el recosteo €/base). Disparador: estabilizar el dato de compras de Llorente29.


---

## DEUDAS TÉCNICAS (⚪ anotadas, con disparador)

- **Fichaje por QR/NFC del local** (anotado 17/06): opción elegante a futuro — un código QR pegado en el local; se ficha escaneándolo, la presencia física la prueba el QR (no el GPS). Elimina el problema de raíz pero es un frente mayor. Disparador: si el modo 'warn' por local no bastara en la práctica.
- **Scripts con expectativas obsoletas** (09/06): `recast-sales.mjs` y `check-reliability.mjs` comparan contra números hardcodeados del 08/06 (pre-canónico) → disparan ⚠️ FALSAS. Actualizar a los valores canónicos actuales: recast 329 casadas/20 no_recipe/0 no_menu_item/0 ambiguous; reliability 95,02 % / 5.979,40 € casado / 6.293 € total / 313,60 € no_recipe.
- **Drift SQL en raíz**: `format_price_per_base.sql` y `supplier_format_prices.sql` sueltos en la raíz del repo → mover a `supabase/migrations/`.
- **`reprocess_sale` y el webhook**: el webhook costea+consume inline (funciona). Cuando entre el 2º TPV (Otter), migrar el webhook a `reprocess_sale` para una sola verdad del post-proceso de frontera. Blindar también `comboProducts` null en `adapt_lastapp_order` (`COALESCE(jsonb_typeof(...)='array' AND ..., false)`).
- **`location_economics.food_cost_coverage_pct`**: se arregló (mig 1300, base total honesta) — verificar que no quedó base casado-only en algún consumidor.
- **`qty_in_base` server-side**; almacén/ubicación operativa por contexto de sesión (no selector manual); `code-splitting` (chunk >500 KB, warning no bloqueante); poblar escandallos base (= frente 1).

---

## HECHO (para no repetir ni olvidar lo ganado)
- HECHO **FOLVY ORDERS — DÍA DE NEGOCIO CON CORTE 04:00, EN PRODUCCIÓN (23/06):** la pestaña Cerrados pasa de una ventana móvil de 6h a DÍA DE NEGOCIO de 04:00 a 04:00 (TZ `accounts.timezone`='Europe/Madrid'); los pedidos vivos sin límite, los terminales solo si su cierre cae en el día de negocio en curso. Diagnóstico: lo que parecía "faltan pedidos en Orders" NO era bug — todas las ventas en BD con `order_status` correcto (0 en NULL), Sales las cuenta bien; el descuadre era reparto por LOCALES distintos + la ventana de 6h escondiendo lo cerrado hace >6h. **Verificado en BD ANTES de tocar** (Code dedujo por código que el backfill dejaba `order_status` NULL → se descartó al comprobar 0 NULL hoy → no se aplicó parche innecesario). Constante `c_business_day_cutoff_hours=4` legible (no configurable aún). Migraciones `20260622T1700_orders_feed_cerrados_dia_local.sql` + `20260623T0900_orders_feed_dia_negocio_corte_0400.sql`, commits 0be164c, 0a1e5b8 (rev-list 0 0). Validado en vivo: pasada medianoche, Cerrados muestra 31 del día. Mejora UX anotada: Orders podría avisar de que muestra UN local (confusión vs Sales que agrega todos).
- HECHO **CLIENTE 2 (Kitchen Grill LstQ) INTEGRADO EN LAST, EN VIVO (23/06):** tienda Ensanche Vallecas (`ff12b2bf`) vinculada en `external_location_map` → cuenta del cliente 2 (`f69e10fa`); integración Folvy instalada en la tienda desde el panel de Last; aparece en el integrador (7 ubicaciones). **VENTAS AISLADAS POR TIENDA verificado en el webhook** (`lastapp-webhook` resuelve `account_id` por `locationId`, no por marca → un Uber/Glovo de esa tienda entra a la cuenta del cliente 2, sin mezclarse con Llorente29). Proceso de alta (invitar correo desde la org del cedente → instalar integración en la tienda) documentado en borrador `docs/folvy_onboarding_cliente_last_cedidas.md`. **DEUDA → AHORA:** la pantalla de "marcas pendientes" del cliente 2 mezcla marcas de Llorente29 (pide catálogo por organización en vez de por `locationId`); no toca ventas; no importar catálogo hasta el fix.
- HECHO **ADAPTADOR HUBRISE — VERIFICADO CONSTRUIDO Y DESPLEGADO (23/06):** RECON de Code confirma `hubrise-webhook` ACTIVE v11 (`verify_jwt=false`), valida `X-HubRise-Hmac-SHA256` (hexdigest + timingSafeEqual sobre body crudo), mapea order create/update a la ingesta canónica (resuelve local por `external_location_map` source='hubrise', marca por `connection_name` vía `external_brand_map`, canal por slug; `upsertSale`→`adapt_hubrise_order`/`close_sale`/`cancel_sale`, idempotente por `external_ref`=order.id), pone `order_status` (alimenta Orders), auto-aceptación fase 2. `hubrise-order-status` (empuje de estado HACIA HubRise) también ACTIVE. Pendientes = comprobaciones de campo con tráfico real (connection_name poblado por marca, completed de Uber/JustEat vía HubRise, cadenas exactas de canal), tax/taxable_base en null (futuro). Correo de seguimiento a Janaina preparado: pide SOLO bloquear la suscripción en "Folvy Test (zy9j2)" (usuario partners@folvy.app), aparca Glovo España hasta tener el test rodando.
- HECHO **ESTACIÓN DE TABLET (`/estacion`) — EN PRODUCCIÓN (21/06):** terminal de cocina a pantalla completa por TOKEN de dispositivo (mismo `kds_device` que el kiosco), 3 pestañas Pedidos · Cocina · Disponibilidad/86 (abre en Pedidos), montada en App.tsx ANTES de los gates de sesión (hermana de `/cocina-tv`). 3 capas: contenedor `TabletStationRoute.tsx` (calca `KdsKioskRoute`; Cocina monta `KdsBoard` con token); Disponibilidad/86 por token (`set_product_availability_by_token` + `availability_panel_by_token` que replica el SELECT sin guard de sesión + `search_products_by_token` + `preview_scope_by_token` + `device_location_by_token`; front `tabletAvailabilityService.ts` + `TabletAvailabilityTab.tsx`); Pedidos por token (`orders_feed_by_token` + `set_order_status_by_token`; `OrdersFeed.tsx` acepta token, polling 10s). QR+URL de alta en `DevicesSettings.tsx` (lib `qrcode`); `manifest-estacion.json` propio apuntado dinámicamente (el icono abre la estación, no el login). Commits hasta `43695e6` (rev-list 0). **DEUDA: las RPC by-token NO versionadas en `supabase/migrations/`.**
- HECHO **IMPRESIÓN FÍSICA AUTOMÁTICA — FUNCIONANDO EN VIVO (21/06):** modelo `printer`/`print_job` multi-transporte AGNÓSTICO (sunmi_cloud/escpos_network/epson_epos/bluetooth/browser_pdf; `config` jsonb lleva ip/port/sn) con RLS calcada de `kds_device`; adaptador ESC/POS (`escpos.js`, corte GS V 1); agente Node.js puente nube↔LAN que DIBUJA el ticket reutilizando el renderizador del front (`ticketRenderer.js`, PORT del `.ts`); RPC `claim_print_jobs`/`report_print_job`/`enqueue_print_job`/`order_for_print` (GRANT a anon las del agente); trigger `tg_auto_print_on_accept` (al aceptar el pedido encola un job ligero por impresora×doc_type → sale el papel SOLO, sin nadie con Folvy abierto). Validado en vivo con la NT311 por LAN (192.168.1.86, puerto 9100). Clave: el proyecto usa el formato nuevo de claves Supabase (`sb_publishable_`; la legacy `eyJ` da 401). **DEUDA CRÍTICA: SQL (printer/print_job, RLS, RPC del agente, order_for_print, trigger) y agente SIN versionar — primero a hacer al retomar.** AHORA: afinar layout de los 3 tickets + botón de reimpresión manual; capa 2 = iconos bitmap.
- HECHO **CICLO DE VIDA DEL PEDIDO — fase 1 EN PRODUCCIÓN, validado en vivo (18/06):** `sale.order_status` = estado del PEDIDO DE PLATAFORMA (new/received/accepted/in_preparation/awaiting_collection/in_delivery/completed/rejected/cancelled/delivery_failed), **separado del `sale.status` contable** (open/closed/cancelled, intacto), con CHECK + siembra desde `raw_tab.status` (migración `20260618T2400_sale_order_status.sql`). **Entrada:** el webhook `hubrise-webhook` espeja `order_status` en cada callback (update/insert vía `common`, cierre antes de `close_sale`, cancelación/rechazo en `cancelByOrderId`) con `mapOrderStatus` (solo valores del CHECK). **Salida:** Edge nuevo `hubrise-order-status` (PUT `/location/orders/:id` a HubRise con cabecera `X-Access-Token`; autoriza al usuario por RLS leyendo la venta con su JWT; espeja `order_status` SOLO si HubRise da 2xx). Token OAuth de la location de pruebas `zy9j2-0` fijado en Secret `HUBRISE_ACCESS_TOKEN` vía flujo `oob` (`location[orders.write]`). **Validado en vivo: #RP65P received→accepted en HubRise + `order_status='accepted'` local.** Commit 5f78aa7 (rev-list 0 0). Investigación HubRise (API ciclo de vida, pausa `order_acceptance`+`resume_at`, inventario/86, variants, benchmark Order Manager de Last) volcada a `docs/folvy_catalogo_publicador_diseno.md` (commit a19b232). Diseño de la pestaña Pedidos en `docs/folvy_pedidos_pestana_diseno.md` (commit 042a506). **DEUDA declarada:** token multi-location = tabla `hubrise_integration` (P-A/CP2); hoy 1 Secret cubre la location de pruebas; el Edge ya lee `external_location_text` para enchufarla sin reescribir. **NOTA:** la "deuda de versionar SQL de HubRise" del cierre anterior YA estaba saldada (`adapt_hubrise_order`/`hubrise_money`/`reprocess_sale` en `20260617T2350`; `close_sale`/`cancel_sale` en `20260613T1900`).
- HECHO **OVERRIDES — precio y margen por canal, EN PRODUCCIÓN (18/06):** el motor `menu_item_economics` estaba MUERTO (INNER JOIN por `channel_id` con todos los productos en canal NULL → no devolvía nada; y un `service_type` global imposible con canales mixtos). Reescrito server-side: `menu_item_channel_economics(p_menu_item_id, p_overrides)` devuelve **una fila por canal** (service_type por canal; precio efectivo preview‖override‖base; comisión `brand_channel_rate`‖`channel_rate`; reproduce `baseFromGross` al céntimo → `contribution_margin` y `net_margin`); `set/clear_menu_item_override` (DEFINER con guard, ON CONFLICT sobre la expresión exacta del índice único). `menuOverrideService.ts` + `EditPricesModal.tsx` (precio por defecto + fila por canal con 86 toggle y **margen neto en vivo** al teclear) + `CatalogProductDetailPage` lee del motor y **jubila el cálculo cliente**. Validado al céntimo contra el cálculo viejo (Glovo 8,12€ / JustEat 7,79€ / Shop 10,65€ / Uber 7,11€ en "Burrito de Tinga de Pollo"). Migración `20260618T2300_menu_item_channel_economics.sql`, commit c1b6d4a. **86 automático por stock** y **precio por LOCAL** = capas declaradas sobre el mismo motor.
- HECHO **MÓDULO ALMACÉN COMPLETO + AvT (TEÓRICO VS REAL) — EN PRODUCCIÓN, verificado en vivo (17/06):** módulo Almacén cerrado con 6 secciones: Resumen (KPIs vivos + tarjetas "Bajo mínimo" con contador real y "Desviación·AvT" encendidas, ya no "Próximamente"), Existencias, **Niveles** (nueva), Movimientos (libro mayor: histórico+entrada directa+traspaso+merma), Inventarios (conteos+autoinventario IA con reparto por persona), **Teórico vs Real**. Más la **ficha del artículo viva**: stock por local + min/par editables + movimientos con filtros de fecha y local. **AvT = EL GRAN DIFERENCIADOR**, en dos formas: (a) PUNTUAL — lee el último `inventory_count` aprobado del local y muestra `system_qty`(teórico) vs `counted_qty`(real) vs `variance_value`(€), ordenado por € perdido, con SALUD DEL DATO arriba (números honestos o ninguno) y CAUSA PROBABLE por línea (dato incompleto/merma real/escandallo no fiable/sin clasificar); % silenciado cuando el teórico ≤ 0 (evita −16100% absurdos). (b) POR PERIODO CONSOLIDADO — RPC `avt_period(account, from, to, location)`: por artículo, inicial(último conteo antes del periodo, o apertura del ledger "estimado" avisando) + compras(recepcion+traspaso_entrada) − consumo(ventas×escandallo) = teórico final; merma = teórico final − conteo real final; universo = artículos con conteo final dentro del periodo; SOLO los medibles (con inicio y fin) suman al total = honestidad. Front pivota una sola RPC en 4 vistas: Local/Almacén/Familia/Artículo + filtro de periodo (mes/mes pasado/30d/90d/trimestre). Validado en Foodint Alcalá: AvT puntual mostró 8 de 10 "Dato incompleto" (stock negativo en saneamiento) = el AvT no miente; AvT periodo "Este mes" = 1 medible, 1 inicio estimado, 148 sin conteo de inicio, Merma 0,00€ (no inventa cifra de los no medibles). Doc `docs/folvy_avt_diseno.md` (benchmark Apicbase/MarketMan/Crunchtime/meez: golea en salud del dato + porqué de la desviación + todo-en-uno). Migración `20260617T2000_avt_period_engine.sql`. Commits e829260, 1e21e53, 699dec1. **Cierra el frente 5 (inventario perpetuo cierra el AvT).**
- HECHO **FRENTE ② NIVELES min/par — BASE DEL MRP II — EN PRODUCCIÓN (17/06):** tabla `stock_level` (item×local, separada del saldo) con 5 campos: `min_qty`, `par_qty` (UI activa) + `reorder_point`, `lead_time_days`, `safety_qty` (listos para el MRP II, no en UI). RPC `stock_levels_overview` (niveles+stock+below_min+to_par_qty, bajo-mínimo primero) y `set_stock_level` (SECURITY DEFINER con guard; gestiona SOLO min/par sin pisar los campos MRP en el upsert — bug de diseño evitado; `?? undefined` + default null = borrar nivel). Pestaña **Niveles** (KPIs bajo-mínimo/con-nivel/sin-definir, filtros, edición inline, "repón X" = par−stock que alimenta el To-Par del order builder) + min/par editables también en la **ficha del artículo** por local (misma tabla/RPC, dos puertas, cero duplicación). El pedido nacerá con `purchase_order.origin='par'` (campo ya existe). Migraciones `20260617T2100_stock_levels.sql` + `T2200_set_stock_level_fix.sql`. Commits 748db53, 7e86e25. **Idea Julio: niveles vivos por consumo (SIGUIENTE 5.bis).**
- HECHO **FICHAJE RESUELTO EN DOS CAPAS (17/06):** problema raíz = trabajadora (Natacha) no podía fichar porque su GPS la situaba a 1.848 m del local estando dentro (GPS por red/WiFi miente), y el radio estaba fijo en 1.000 m en el código. **(a) FICHAJE MANUAL FIABLE del manager:** `handleClock` en `StaffPage` (pestaña Fichajes del modal de empleado) reescrito — antes solo tocaba estado local (no escribía en BD); ahora escribe vía `addClockEntry` (→ `clock_entries`), marca `source='manual'` + MOTIVO obligatorio + quién (en `address`, rastro legal), sin GPS del manager, sin bloqueo por horario (redondeo solo informativo). Commit b176bf3. **(b) FICHAJE DE RAÍZ — radio y modo por local:** `locations` gana `clock_radius_m` (default 200) y `clock_geofence_mode` ('block'|'warn'); `FichajeEmpleado` lee radio/modo del local (no el 1000 fijo); modo 'warn' deja fichar aunque el GPS sitúe fuera del radio, marcando "Fuera de zona · Xm" para revisión (resuelve a Natacha sin manual y cubre Plaza Castilla/Carabanchel que no tienen coords); UI en `OtherPages`/LocationsPage (radio + selector Bloquear/Avisar). Migración `20260617T2300_clock_geofence_config.sql`. Commit 8cdb558. **PENDIENTE: Julio configura Foodint Alcalá en modo 'warn' y valida que Natacha ficha sola. La salida de Natacha de hoy ya se puede hacer con el botón manual.**
- HECHO **MOTOR DE APROVISIONAMIENTO RECONSTRUIDO A €/UNIDAD BASE + TRES ALARMAS DE PRECIO — EN PRODUCCIÓN, coste-neutral verificado (16/06):** `article_supplier.last_price` pasa de €/formato a **€/unidad base** (€/g, €/ml, €/ud) en las dos cuentas (Folvy Interno + Llorente29 Food); cierra de raíz el OBLIGATORIO D (la ambigüedad €/caja-pedidos vs €/base-Kitchen, caso Delicias-COHELDI). `kitchen_recompute_raw_cost` lee €/base directo (desacoplado del formato). **Escritores a €/base** vía `_eur_base_from_format` (`confirm_goods_receipt`, `learn_from_receipt`, `apply_invoice_costs`) → el bug del doble (qty×precio) muere por construcción. **UI base-first**: ficha + alta teclean/muestran €/kg (helpers `unitPriceToBase`/`unitPriceFromBase`/`pickDisplayUnit`); selector de estrategia (4 reales: last_purchase/average_weighted/average_window/fixed); **alta blindada** (¿peso/volumen/unidades? → base fina, imposible base no-fina, fin del bug del aceite); botón **"Recostear todo"** (`recostAllRaws`, Folvy Interno 183/183, Llorente29 98/98). **Precio PACTADO** (`article_supplier.negotiated_price` €/base, editable "pactado", no afecta al coste = referencia). **Tres alarmas de precio en recepción, independientes, legibles en €/kg junto al precio**: (1) salto puntual vs último (`priceAlertFor`, `price_alert_pct`); (2) contra pactado (`negotiatedAlertFor`, `negotiated_alert_pct`=0); (3) deriva acumulada vs mediana del periodo (`price_drift_for`+`driftAlertFor`, `drift_alert_pct`=25, `drift_window_months`=6, mínimo 3 recepciones). €/base entrante con UNA fórmula (`lineActualPerBase`: OCR por importe / manual por precio÷qty_in_base); sin cantidad >0 callan las del entrante (la deriva no). **Ningún competidor junta las tres.** Fixes de raíz: `supplier_format_prices` arreglada a €/base (devolvía heredado → alarma marcaba 230000%); `void_goods_receipt` revierte stock Y last_price (recepción superviviente o NULL) + recostea; `price_drift_for` ignora anuladas. **SQL versionado**: `supabase/migrations/20260616T1200_aprovisionamiento_eur_base.sql` (8 funciones + columnas, idempotente); `database.ts` regenerado. **PRÓXIMA pieza = SIGUIENTE 11 (Bloque 6c, vista de tendencias).** Deuda menor: chunk >500 kB (warning conocido).
- HECHO **CAMBIOS DE TURNO del trabajador — arreglado (16/06):** `fetchColleagues(locationIds)` en `supabaseSync.ts` (busca compañeros por local, la RLS filtra por cuenta; reemplaza `fetchEmployees(null)` que devolvía `[]` por `if(!accountId) return []`) + `SolicitarCambioModal`/`TablonCambiosView`/`MisCambiosView`. El "bloqueante" de login del trabajador (`El empleado no pertenece a tu cuenta`) era **FALSO**: sesión cruzada de admin cacheada en el navegador de localhost; en producción funcionaba (verificado en localhost incógnito con credenciales limpias). Commit `9af0a0c` (rev-list 0 0). **PENDIENTE: validar la confirmación de INTERCAMBIO con un trabajador real en producción (validación, no deuda).**
- HECHO **MOTOR DE INVENTARIO ARREGLADO DE RAÍZ + STOCK EN PEDIDOS — EN PRODUCCIÓN, verificado de punta a punta (14/06):** Bug raíz: `apply_inventory_count` calculaba `delta = counted_qty − system_qty`, y `system_qty` llegaba NULL en cuenta nueva (anterior al `COALESCE` de `build_inventory_count`) → `45000 − NULL = NULL` → el filtro `<> 0` no se cumplía → NO insertaba asiento de apertura/ajuste → el stock quedaba SOLO con los `consumo` de ventas → negativos absurdos (Patatas −5.340). **Principio rector (Julio): un conteo NO suma ni resta, SUSTITUYE — fija el stock a lo contado EN SU INSTANTE, y las ventas posteriores restan encima.** Arreglo `apply_inventory_count` v3: `delta = contado − SUM(qty_base del ledger con occurred_at < instante_del_conteo)`; asiento anclado en `occurred_at = COALESCE(started_at, closed_at, created_at)` (NO `now()`); idempotente (borra asientos previos del conteo antes de recalcular → re-aplicar no duplica, resuelve el caso Arroz). **APERTURA (is_opening) NO aplica chequeo de tolerancia** (es punto de partida, no hay stock previo): arreglado en backend v3 Y en front (`InventoryCountSheet.tsx`: `missingReasons = isOpening ? 0 : …` — destraba botón, guard y texto a la vez). Re-aplicado INV-00002 como Julio (98 asientos; Patatas 43.530 = 45.000 contado − 1.470 ventas posteriores, Arroz 27.000 sin duplicar). Apertura INV-00003 aprobada DESDE LA APP con sesión real (Aceite 780, resto intacto) = flujo verificado de punta a punta. **STOCK REAL en pantalla de pedidos** (`supplierCatalogService` lee `recipe_item_location_stock` del local activo; helper `formatStockForOrder` convierte base→cajas como tspoon "4,4 caja"; `SupplyOrderBuilder` pasa locationId y pinta la columna). Commits cca7905, 7732a6b, 1b9b30d, 540393f (todos rev-list 0). DEUDA: versionar `apply_inventory_count` v3 en `supabase/migrations/`; `build_inventory_count` aún rellena `system_qty` (ya no se usa, limpiar).
- HECHO **FEATURE-GATE → CUENTA REAL + FICHAJES + UBICACIÓN (14/06):** `featureGateService.ts` ya no fuerza a admin al sandbox `00000000-…0001` (usaba `ACCOUNT_ID_FOLVY` si role=admin → Julio caía al sandbox, 406 RLS, React #300) — ahora usa `profileData.account_id` real con respaldo manager_locations. **PUENTE TEMPORAL:** `accounts.is_internal=true` en Llorente29 (51ad1792-…) porque es la PRIMERA cuenta no-interna que estrena la app y el gating de módulos (account_modules/G-7) NO está construido — DEUDA con disparador: quitar el puente al construir aprovisionamiento de cuenta/onboarding + blindar el guard del #300. **Fichajes:** las 3 trabajadoras fichando — coords reales de Foodint Alcalá (40.4345672/-3.6528093), radio 200→1000m en `FichajeEmpleado.tsx` + `fichajeKiosko.ts` (el radio del kiosko vivía en localStorage del admin, no llegaba al móvil; DEUDA: mover a `locations`). **`OtherPages.tsx` (LocationsPage):** geolocalización siempre visible + botón "Usar mi ubicación actual" + lat/lng editables (campos `lat`/`lng`). Plaza Castilla y Carabanchel siguen sin coords.
- HECHO **MÓDULO KDS COMPLETO — EN PRODUCCIÓN, verificado en vivo (13/06):** módulo nuevo `src/modules/kds/` (Folvy KDS, topBarOrder 7). 8 migraciones (2100→2800) + helper `safe_jsonb` (2750). **Ciclo de vida de la venta** (0a/0b): `sale.status` open→closed→cancelled; coste+consumo SOLO en `tab:closed`; motor canónico agnóstico de origen; webhook reescrito como frontera fina. **Capa 1:** topología (kitchen_station prep/expo + ruteo familia→estación + override + estación por defecto), estado (bump por estación, marcado por plato reversible, dispositivos multi-tablet token revocable), RPC doble puerta sesión/token (kds_board, kds_bump/unbump, kds_mark_line, kds_recipe, kds_set_default_station atómica, kds_resolve_device, kds_authorize). **Tablero por estado de COCINA** (no contable): el pedido sale cuando el expo marca servido (botón **Servir**, doble toque, cierra el ticket); ventana 2h; canal por COALESCE. **Bloqueante resuelto:** los 74 platos sin family_id caían en "Sin estación" → fallback a estación por defecto del local (0 huérfanas). **Cook Mode:** ingredientes base+escalado, alérgenos de ficha, foto. **Ticket completo:** combos desglosados (cabecera atenuada + componentes en grande), modificadores sangrados, NOTAS DE CLIENTE (de raw_tab.products[].comments por organizationProductId; banda roja ⚠ por plato; raras=peligrosas, por alérgenos); `safe_jsonb` blinda el cast. **Kiosco** en ruta propia `/cocina-tv` (el Shell monta módulos en raíz sin slug → startsWith('/kds') secuestraba el módulo). Verificado en vivo: combos desglosados, servir quita el ticket, estación por defecto, Cook Mode. Commits 4f1c8d6, 3ccaab3, d542a8f, cbbcc77, 8867e39, 733c36b, 9290215, 94d43f4, df5e7c6 (todos rev-list 0). Benchmark: iguala la base de Toast/Square/Fresh KDS y golea con coste/margen vivo + alérgenos de ficha + Cook Mode (receta+pasos al pinchar) + multimarca por UUID. **PENDIENTE: Nivel 2 (impacto modificador receta/coste) en SIGUIENTE 6.bis; cancelación real sin probar; regenerar database.ts.**
- HECHO **FUNCIÓN ESTRELLA — importar escandallo por FOTO/PDF/EXCEL/WORD — EN PRODUCCIÓN (12/06):** el motor (Edge `extract-recipe` + RPC `materialize_recipe_session` + `run_mapping`) ya existía vivo; faltaba la UI ("G7" nunca cableado). Construido: `recipeImportService.ts` (detecta formato; imagen comprime+sube, PDF sube tal cual → `kind:'photo'` visión con bloque document+header beta pdfs; Excel→SheetJS sheet_to_csv y Word→mammoth extractRawText → texto → `kind:'conversational'`); Edge reescrito para distinguir visión/texto; botón "Importar ficha" en `KitchenRecipesPage` (accept img+pdf+xlsx+xls+csv+docx) + modal. Probado foto en vivo (American Fries, 3 ingr, 0,29€). **Diferenciador: meez/Apicbase obligan a teclear; Folvy lo monta desde una foto de la ficha del cocinero.** Commits 67dd3f3, 1b3080c.
- HECHO **COMPLETADO MASIVO IA de ingredientes (12/06):** botón "Completar N con IA" en `KitchenItemsPage` (si hay pendientes) + `recipeBulkEnrichService.ts` (serie, pausa+backoff ante saturación, reutiliza `enrichIngredient`+`applyEnrichment`, captura errores). Resultado Llorente29: 76→31 terminados. FIXES de raíz descubiertos: `extractJson` robusto en `enrich-ingredient` (extrae objeto {...} aunque la IA añada prosa — recuperó la mayoría de fallos); CHECK de `recipe_item_allergen` alineado al código (state `contains/may_contain/free/unknown`, source +`ai_enrich` — antes 400 masivo por vocabulario desalineado); `applyEnrichment` reordenado (familia/IVA primero, alérgenos delete+insert no bloqueantes — antes el 400/409 de alérgenos cortaba el cierre). **DEUDA: el cierre por `review_notes` queda como OBLIGATORIO C arriba.**
- HECHO **PWA INSTALABLE (12/06):** iconos PNG (192/512 + maskable 192/512 safe-zone 82% + apple-touch 180) desde isotipo manager; `manifest.json` corregido (PNG con sizes reales, antes SVG → iOS no instalaba); `index.html` apple-touch a PNG; `sw.js` mínimo passthrough + registro en `main.tsx` (solo PROD); `InstallAppButton.tsx` en `HomeEmpleado` (Android prompt nativo, iOS modal instrucciones). **DEUDA: que el botón instale directo en Android (OBLIGATORIO B).**
- HECHO **ACCESO TRABAJADOR — arreglo mínimo (12/06):** worker puro sin botón "salir" (evita atrapamiento por QR de un solo uso + signOut). Completo (PIN) = OBLIGATORIO A.

- HECHO **AUTOINVENTARIO IA A1+A2 — EN PRODUCCIÓN (10/06):** motor de 2 capas. QUÉ contar = score independiente por valor (`stock_value`) + rotación (consumo €) + riesgo (varianza+merma), normalizado 0-1 por location, pesos 0.35/0.35/0.30. CUÁNTO = COBERTURA de valor en riesgo (un control: target de cobertura, defecto 80%), NO cadencia fija. Criticidad operativa = override HARD (`is_operational_critical`/`operational_min_qty`), no peso. Función `autoinventory_queue` (SECURITY DEFINER, guard admin/manager). Front: `autoinventoryService.ts` + `AutoInventorySection.tsx` + pestaña "Autoinventario" en `InventoryPage.tsx`. Verificado en vivo (Foodint Alcalá): 24 de 187 a contar, 79,2% valor cubierto; Pan Hamburguesa (#1, rota) gana a Milanesa Pollo (#2, 322€ sin rotación) → prueba el objetivo rotación-sobre-valor. Commits autoinventory_queue + 94719c6. Benchmark tspoon: tspoon NO tiene ABC/score/riesgo (conteos manuales por zona) → Folvy golea. DEUDA declarada: `build/close_inventory_count` mantienen su `abc_class` percentil (foto histórica); convergen a beber de A1 cuando esté validado en uso. **A3 (cola del día por frescura de cobertura) diseñada, NO construida** (freshness = días desde último conteo aprobado; "zona del día con trampa" por `storage_area`).
- HECHO **ONBOARDING INTEGRACIONES Last.app — herramienta interna (10/06):** sección "Integraciones Last.app" en la ficha de cliente (`CuentaDetallePage`) vía `IntegrationsSection.tsx` + `lastappIntegrationService.ts`. Orquesta: alta de integración (org + secret + tipo propia/cedida), vincular tiendas Last→locales, importar catálogo (Edge `lastapp-catalog-import`), sembrar escandallos + recasar. Resumen del import visible (productos/combos/marcas resueltas y sin resolver) en simulación e import. Commits d7ebc7a, 0347bb0. **Aprendizaje CTB (banco de pruebas):** el import de Last falla cuando el catálogo de una marca está organizado POR CANAL sin "default" (las cedidas de Cloudtown) → 0 productos con "ok". Las propias tienen "default" + variantes por canal → entran. El casado de marca depende de `lastapp_catalog_product` (que el import puebla) — sin él, `no_brand`. Esto destapó el frente de ingesta canónica.
- HECHO **DIAGNÓSTICO + DISEÑO INGESTA CANÓNICA (10/06):** fiabilidad real ~87% (426 casadas / 76 sin casar; `no_brand` 56 = cedidas). Diseñada la arquitectura "Folvy es la verdad, TPV = adaptadores reconciliados, cola de excepciones = 100%" en `docs/folvy_ingesta_canonica_diseno.md`, coronando `folvy_fiabilidad_casado_diseno.md`. 8 decisiones cerradas (regímenes propias/cedidas, tres capas artículo/presentación/matrícula, casado por id estable multi-fuente, pending_review, catálogo vivo). Contratos de Last verificados con su OpenAPI v2.0.0 (`catalog:updated` = notificación de `catalogIds[]`; línea de venta trae `organizationProductId`+`catalogProductId`+`externalId`+`locationBrandId`). Hallazgo: `locationBrandId` viene poblado en el contrato de Last pero NULL en las ventas CTB → se pierde en la ingesta; capturarlo haría el casado de marca determinista. NADA construido (diseño).
- HECHO **RESEARCH ESTRATEGIA DELIVERY (10/06):** mapa completo de proveedores en `docs/folvy_estrategia_delivery.md`. Otter = candidato líder (Glovo España verificado, no compite con Folvy, API de partner completa camuflable). Correo de partnership a Otter ENVIADO. Glovo directo (DH API) en cola sin fecha. Mapa: HubRise (limpio, sin Glovo ES), Deliverect/GrubTech/Ordatic (Glovo pero compiten), KitchenHub (sin Glovo ES), GetOrder (sin verificar).

- HECHO **CATÁLOGO LLORENTE29 COMPLETO desde Last (21/06):** importador `scripts/import-last-catalog.mjs` arreglado (4 bugs: cruce de marcas, sin categoría, producto compartido entre marcas, combos vacíos) + app muestra combos en su categoría con foto/editables. 17 marcas completas y fiables idénticas a Last (8 cedidas + 9 propias), todas `sin_categoria=0`, con fotos/categorías/combos/modificadores. Método validado y aplicado a escala por org (borrar transacción → `--run` org → verificar → recast). Casado 87,8% estable. Commit 24f7961. **DESBLOQUEA 86 + HubRise cliente 2 + escandallos.** Reutilizable para cliente 2 (cedidas CTB, misma org Cloudtown). DEUDA menor: detalle del combo no lista aún sus componentes.
- HECHO Folvy Kitchen (escandallos, coste a la décima, recompute cascada).
- HECHO Recipe Steps E8 (pasos enlazados a ingredientes) — diferenciador vs tspoon.
- HECHO Last.app webhook (ventas automáticas).
- HECHO Casado de ventas lastapp arreglado (07/06): cache por `brand_id|recipe_item_id`, marca vía `catalogProductId`.
- HECHO **MODELO CANÓNICO multi-TPV (08/06):** puerta única de entrada. El core NO lee formato de ningún TPV; solo el adaptador. `adapt_lastapp_order` descompone raw_products en jerarquía product/modifier/combo_item. Añadir un TPV = 1 adaptador + mapeos, CERO cambios en core. **Principio rector 5: autorización en la frontera, motor puro.**
- HECHO **MOTOR DE COSTE DE VENTA REAL (08/06):** `sale_line.computed_cost` = escandallo ± modificadores confirmados + combos (Σ componentes). Lee canónico. Coste de venta CONGELADO (margen histórico inmutable).
- HECHO **G3 MODIFICADORES — COMPLETO (08/06):** `modifier_recipe_impact` con ciclo de vida (proposed/confirmed/rejected); solo `confirmed` toca el coste (IA propone, humano decide). Pestaña en el editor, Edge `propose-modifier-impacts` (IA), latido de coste en vivo. Niveles 1-2 operativos; 3 dormido hasta histórico.
- HECHO **SUBSISTEMA DE FIABILIDAD — capa 4 SEÑAL CENTRAL (08/06, verificada 09/06):** RPC `sales_mapping_reliability(account, from, to)` con denominador HONESTO (casado / total por importe), desglose no_recipe (dinero a oscuras) vs no_menu_item (calculable) → golea a tspoon que los junta. Umbral `kitchen_settings.reliability_min_pct` (defecto 90). Front: `salesReliabilityService.ts` + `SalesExceptionsPage.tsx` + bloque en `KitchenMenuPage.tsx`. Verificado en vivo: 95,02 % verde. Identidad en el adaptador, fiabilidad lee canónico (agnóstica de TPV).
- HECHO **MOTOR DE CONSUMO TEÓRICO — VIVO (09/06):** ventas × escandallo → `stock_movement` tipo `consumo` (qty negativa, unit_cost sellado). Funciones puras: `explode_recipe_to_raws` (recursiva, parada en raw/stockable), `compute_sale_line_consumption` (gemelo de `compute_sale_line_cost`: product/combo/modifier confirmado), `recompute_sales_consumption` (frontera con guard), `recompute_location_stock_core` (puro) + wrapper. Paridad consumo==coste validada al céntimo (simple+combo). Webhook cableado (paso 4, resiliente). UI: pestaña "Consumo" en Supply/Inventario (rango + recalcular + consumo por ingrediente, € desc). Benchmark: **paridad** con R365/Crunchtime/MarketMan; borde real = teórico CON MODIFICADORES (el punto ciego del sector). Commit af85058.
- HECHO **JUBILAR IDENTIDAD DEL RECAST + `reprocess_sale` UNIFICADO (09/06):** `recast_lastapp_sales` reescrito — casa por el canónico (bucle `reprocess_sale`), ya NO lee `raw_products`; conserva la auto-propagación multimarca (pieza B) y la firma/métricas exactas (los dos llamadores `resolve_unmapped_sales`/`classify_unmapped_product` y el script siguen funcionando). `reprocess_sale(sale_id)` = post-proceso unificado de frontera (adapt + coste + consumo) con LIMPIEZA de consumo por venta ANTES de re-adaptar → arregla el consumo FÓSIL que dejaba la recreación de `sale_line` con IDs nuevos (`stock_movement.source_id` sin FK). Idempotencia demostrada (2 corridas → 1511 movimientos idéntico, 0 fósiles). La pieza B mejoró el casado (186→209 líneas con consumo). Commit 6e7f765.
- HECHO **RECOSTEO + POLÍTICA CONGELADO/VIVO (09/06):** diagnóstico demostró que NO hay ventas mal costeadas — el motor está sano; los NULL/0 son legítimos (falta escandallo o precio de reventa → frente 1). **Modelo de DOS RELOJES confirmado como decisión de producto**: `sale_line.computed_cost` CONGELADO (margen histórico, inmutable, auditable) / `recipe_item.computed_cost` VIVO (consumo, previsión, margen futuro). Son dos relojes a propósito; no se comparan como si coincidieran. El recast recostea con el coste VIVO (corrección deliberada); el flujo normal (venta entra por webhook → congela → no se vuelve a tocar) preserva el histórico. Sin código nuevo.
- HECHO Folvy AI v1++ (streaming, ve 3 módulos).
- HECHO APPCC (corrección + foto + notificación) — diferenciador.
- HECHO Supply: pedido sobre catálogo (3 modos, multi-local, PDF, PED-correlativo). Recepción C2.2 OCR. C3 factura + three-way (pendiente probar vivo). **Recepción ESPEJO DEL ALBARAN (9/06, commits `5230ff4..62a225e`): recibido a ciegas, foto del albarán a la izquierda, "el albarán dice" agrupado, formato en una línea con pack real (Caja N×interior vía `ensurePackTree`, total derivado, sin trigger en cascada), "→ X al almacén" visible, rojo+motivo si no cuadra. PENDIENTE VALIDAR con varios albaranes (checkpoint 0 de AHORA).**
- HECHO Motor de IVA versionado por fecha.
- HECHO Inventario perpetuo capa 1 (crear→contar ciego→cerrar→aprobar→ajuste).
- HECHO Web pública folvy.app (7 páginas EN/ES).
- HECHO Auditoría competitiva (tspoon a fondo + mapa competitivo mundial).

---

## Regla de oro del guion
**No empieces una sesión preguntándote qué hacer. Abre este documento: el frente 1 de AHORA es lo que toca.** Si algo cambió las prioridades, se reordena aquí — siempre con la pregunta: *¿qué acerca más a Llorente29 en producción, que es lo que dispara las ventas?*
